# CredoBounty 진행 상황

> 이 문서는 "무엇을 왜 이렇게 만들었는지"를 남기는 개발 일지다.
> 코드 주석은 각 파일의 지엽적인 설계 이유를, 이 문서는 프로젝트 전체 흐름과 의사결정을 기록한다.

## 0. 프로젝트 개요
- 원본 기획: `docs/기획서_원본.pdf` (68페이지, CredoBounty 마스터 기획서)
- 컨셉: 고관여 전문 결과물(백엔드 튜닝, 스마트컨트랙트 감사, 건축물 하자진단, 계약서 검토 등)을
  검증된 전문가와 의뢰인이 에스크로 결제로 안전하게 거래하는 플랫폼
- 포지셔닝: 기획/설계는 본인이 주도, 구현은 AI(Claude)와 협업 — PM 관점 포트폴리오

## 1. 기술 스택 결정 이력
- **1차 결정**: Spring Boot + JPA + PostgreSQL (React+Vite) — 본인이 직접 학습하며 코딩할 계획이었을 때
- **최종 결정**: 기획서 원안대로 **Next.js + NestJS + TypeScript + PostgreSQL**
  - 이유: 본인이 코드를 직접 작성/유지보수하지 않고 PM 역할에 집중하기로 방향을 정하면서,
    "학습 부담이 적은 스택"이라는 1차 결정의 전제가 사라짐
  - 오히려 기획서에 명시된 스택 그대로 구현하는 것이 "설계한 대로 정확히 구현을 지시하고
    검수했다"는 실행력 스토리에 부합
  - TypeScript 풀스택 + Vercel/Render 무료 배포로 라이브 데모 링크 확보가 쉬움

## 2. 외부 연동 원칙
본인인증(PASS)·오픈뱅킹 에스크로·AWS S3는 전부 **Mock 어댑터**(`backend/src/mocks/`)로 구현했다.
기획서 7장 "Plan B: API 지연 대비"에 이미 명시된 표준적인 접근이며, 실제 연동이 생기면
Mock 클래스의 내부 구현만 교체하고 인터페이스(메서드 시그니처)는 그대로 유지하도록 설계했다.

## 3. 원본 ERD 대비 추가/변경한 부분
기획서 10장 ERD(Users/Certifications/Bounties/Transactions/Disputes)를 기본으로 하되,
실제로 동작하는 흐름을 만들기 위해 아래 2개 테이블을 추가했다 (기획서 텍스트에서 존재가
암시되었으나 다이어그램에는 없던 부분):
- `bounty_applications`: 8장 "전문가 지원" 단계를 구현하려면 "누가 지원했는지" 목록이 필요
- `bounty_submissions`: 9장 "artifacts" 언급에 대응하는 결과물 제출 기록

## 4. 백엔드 구현 완료 (Phase 1)
- [x] 회원가입/로그인 (JWT), Mock CI 생성으로 1인 1계정 원칙 재현
- [x] 전문가 인증 신청 + Mock 승인/반려
- [x] 바운티 등록/목록/상세
- [x] 전문가 지원 → 의뢰인이 지원자 선택 → LOCKED 전환 + Mock 에스크로 락업
- [x] 결과물 제출(Mock S3 업로드) → SUBMITTED 전환
- [x] 의뢰인 승인 → 플랫폼 수수료(10%) 반영 정산 → SETTLED 전환
- [x] 이의제기 접수 → DISPUTED 전환 + 자금 FROZEN, 관리자 중재(환불/정산) API
- [x] curl로 전체 흐름(회원가입→인증→바운티→에스크로→정산) 엔드투엔드 테스트 통과

### 발견하고 고친 버그
- `Bounty.assignedExpertId`, `Transaction.receiverId`를 `string | null`로 선언하면
  TypeORM이 컬럼 타입을 추론하지 못해 부팅 실패 → `type: 'uuid'` 명시로 해결
- `JwtModule.register()`에 `process.env.JWT_SECRET`을 직접 넣으면, 이 값이 모듈 파일이
  import되는 시점(= `ConfigModule.forRoot()`가 .env를 로드하기 전)에 평가되어 fallback
  시크릿으로 고정돼버리는 반면 `JwtStrategy`는 인스턴스 생성 시점(로드 이후)에 값을 읽어
  서로 다른 시크릿을 쓰게 됨 → 모든 인증 요청이 401 → `JwtModule.registerAsync + useFactory`로
  두 곳 모두 "인스턴스 생성 시점"에 값을 읽도록 통일해 해결

## 5. 프론트엔드 구현 완료 (Phase 1)
- [x] Next.js 14 App Router + Tailwind, `axios` 인스턴스 + 요청 인터셉터로 JWT 자동 첨부
- [x] `next.config.js`의 `rewrites()`로 `/api/*`, `/uploads/*`를 백엔드(8080)로 프록시
  → 브라우저 입장에서는 프론트/백엔드가 "같은 origin"으로 보여서 CORS 신경 안 써도 됨
- [x] 로그인/회원가입 화면, 전역 인증 상태(`AuthProvider`/`useAuth`, `localStorage` 토큰)
- [x] 전문가 인증 신청 화면 (도메인/트랙 선택 + 증빙번호 제출 + 내 인증 내역)
- [x] 바운티 목록/상세/등록 화면, 상태별(PENDING~SETTLED) 배지(`StatusBadge`)
- [x] 바운티 상세 화면에서 보는 사람이 누구냐(의뢰인/선정된 전문가/제3자)에 따라
      지원하기·선정하기·제출하기·승인하기 버튼이 다르게 노출되도록 조건부 렌더링
- [x] `npm run build` 전체 라우트(8개) 타입 에러 없이 빌드 성공
- [x] Next.js `14.2.15` → `14.2.35`로 패치 업그레이드 (npm audit 취약점 대응).
      남은 2건(high/critical)은 `next/image` 최적화 서버, 커스텀 서버, Server Actions 등
      **이 프로젝트가 쓰지 않는 기능**에 대한 광범위한 경고라서, Next 16 전면 업그레이드(호환성
      깨짐 위험)보다는 지금 단계에서는 수용하기로 결정. Phase 2 배포 전 재검토 예정.

### 발견하고 고친 버그 (추가)
- `POST /bounties/:id/submit`에 파일(`resultFile`)을 첨부하지 않고 요청하면
  `file`이 `undefined`가 되어 `file.originalname`에서 예외가 터지고 그대로
  `500 Internal Server Error`가 나가버림 → 컨트롤러에 `if (!file) throw new
  BadRequestException(...)` 가드를 추가해서 원인을 알 수 있는 `400` 에러로 변경

## 6. 통합 테스트 (프론트엔드 프록시 경유)
백엔드에 직접 curl로 붙인 테스트(Phase 1)에 더해, **실제 화면이 쓰는 경로와 동일하게**
`http://localhost:3000/api/...` (Next.js 프록시)를 통해 전체 흐름을 재검증했다:
회원가입 → 로그인 → 내 정보 조회 → 바운티 등록(의뢰인) → 전문가 회원가입/인증신청 →
바운티 지원 → 지원자 선정(에스크로 락업) → 결과물 제출(멀티파트 파일 첨부) → 승인/정산.
300,000원 바운티 기준 플랫폼 수수료 30,000원이 정확히 계산되어 전문가에게 270,000원
정산되는 것까지 확인. **파일 없이 제출 시도 → 400 에러로 안전하게 막히는 것**도 함께 확인.

## 7. 목표 재정의: 포트폴리오 시연 → 실제 상용화
Phase 1 완료 시점까지는 "논리적으로 맞게 동작하는 시연용 프로토타입"에 집중했다.
이후 목표를 "실제 코드 작업까지 완성해 테스트하고, 다른 기업에 판매하거나 취업에
쓸 수 있는 상용 서비스로 만든다"로 재정의했다. 이에 따라 우선순위를 다음과 같이 나눈다.

- **지금 바로 코드로 해결 가능한 것**: 관리자 권한 분리, 비밀번호 재설정/이메일 인증,
  에러 처리 강화, 실제 AWS S3 연동 전환 — Phase 2 작업 목록
- **코드만으로는 해결 안 되는 것**: 본인인증(PASS)·실제 돈이 오가는 에스크로 결제는
  사업자등록 + PG사/본인인증기관과의 정식 계약 + 관련 금융 라이선스가 필요한 영역이다.
  Mock 어댑터 인터페이스는 실제 연동으로 그대로 교체 가능하게 설계해뒀지만, "진짜 돈을
  예치하는 상용 서비스"가 되려면 이 사업적 결정이 선행되어야 한다. 그 전까지는
  "데모/베타 모드"임을 명확히 표시한 채 운영하는 것이 현실적인 경로.

## 8. Phase 2 진행 상황
- [x] 관리자(ADMIN) 역할 분리
  - `UserRole`에 `ADMIN` 추가. 공개 회원가입(`POST /auth/register`)은
    `PUBLIC_REGISTRABLE_ROLES`(CLIENT/EXPERT/HYBRID) 화이트리스트로 제한해서
    누구나 role:"ADMIN"을 보내 관리자가 되는 것을 원천 차단 (테스트로 400 확인)
  - `@Roles(...)` 데코레이터 + `RolesGuard` 구현 (Nest 표준 RBAC 패턴).
    `JwtAuthGuard` → `RolesGuard` 순서로 적용해야 함 (로그인 확인 → 역할 확인)
  - `POST /disputes/:id/resolve`(이의제기 중재)에 `@Roles(UserRole.ADMIN)` 적용.
    일반 사용자가 호출하면 403, 관리자는 정상 처리되는 것을 실제 시나리오로 확인
  - 관리자 계정은 API로 만들 수 없고, 서버 터미널에서만 실행 가능한
    `npm run seed:admin` 스크립트(`src/scripts/seed-admin.ts`)로만 생성/승격 가능
- [x] 비밀번호 재설정 / 이메일 인증 기능
  - `AuthToken` 엔티티 하나로 두 기능(비밀번호 재설정/이메일 인증)을 `purpose`
    구분으로 함께 처리 (같은 "토큰 발급→메일 전송→검증" 뼈대를 재사용)
  - 원본 토큰은 이메일로만 보내고 DB에는 sha256 해시만 저장 (탈취 대비)
  - 토큰은 1회용(사용 즉시 usedAt 기록) + 만료시간(재설정 30분/인증 24시간) 적용
  - 비밀번호 재설정 요청은 가입 여부와 무관하게 항상 같은 응답 → 이메일 존재
    여부가 새어나가는 것(enumeration 공격)을 차단
  - `MockEmailService` 추가 (콘솔 로그로 링크 출력, 나중에 AWS SES 등으로 교체 가능)
  - 프론트: `/forgot-password`, `/reset-password`, `/verify-email` 화면 추가,
    로그인 화면에 "비밀번호를 잊으셨나요?" 링크 연결
  - 토큰 재사용 차단, 가짜 토큰 차단, 재설정 후 새 비밀번호 로그인까지 시나리오
    테스트로 검증 완료
- [x] 에러 처리 및 입력값 검증 강화
  - `AllExceptionsFilter`(전역 예외 필터) 추가 — 앱 어디서 어떤 에러가 나든
    항상 `{ statusCode, error, message, path, timestamp }` 형태로 통일해서 응답
  - **발견하고 고친 버그**: `GET /bounties/:id`처럼 UUID를 받는 API에 UUID가
    아닌 문자열(`/bounties/이상한값`)을 넣으면, PostgreSQL이 던지는
    `QueryFailedError`(형식 오류, 코드 22P02)를 아무도 안 잡아서 500 Internal
    Server Error가 그대로 나가고 있었다 → 필터에서 이 에러코드를 감지해
    400 Bad Request + "요청 형식이 올바르지 않습니다"로 변경
  - 500번대(진짜 예상 못한 에러)는 서버 로그에 스택 트레이스를 남기되,
    사용자에게는 내부 구조가 드러나지 않는 일반 메시지만 응답하도록 분리
  - 잘못된 ID 형식/존재하지 않는 리소스(404)/DTO 검증 실패(400 배열)/
    인증 없음(401)/권한 없음(403)/존재하지 않는 라우트(404) 각각 정상적으로
    구분되는지 시나리오 테스트로 확인
- [x] 파일 저장 실제 AWS S3 연동 전환 준비
  - `StorageService`(추상 클래스를 DI 토큰으로 쓰는 NestJS 표준 기법)로 계약을
    분리하고, 컨트롤러는 `MockStorageService`가 아니라 이 계약에만 의존하도록 변경
  - `S3StorageService` 구현체 추가 (`@aws-sdk/client-s3`) — `.env`에
    `STORAGE_DRIVER=s3` + AWS 자격증명만 채우면 즉시 전환되도록 준비 완료.
    지금은 AWS 계정이 없어 기본값(Mock)으로 동작 중
  - 구현체 선택 로직은 반드시 `useFactory` **함수 본문 안에서** `process.env`를
    읽도록 작성 — JWT_SECRET 때와 같은 "모듈 import 시점에 env를 먼저 읽어버리는"
    버그 재발 방지
  - 확장자/용량 검증 로직을 `file-validation.util.ts`로 분리해 Mock/S3 구현체가
    동일한 규칙을 공유하도록 정리
  - `npm install @aws-sdk/client-s3` 과정에서 npm audit 취약점이 늘어난 것처럼
    보이지만, 실제로는 기존 `bcrypt`의 네이티브 바이너리 설치 도구
    (`node-pre-gyp`→`tar`) 체인이라 이번 추가와는 무관 — `npm install` 시점에만
    쓰이는 도구라 런타임에는 영향 없음으로 판단, 별도 조치는 보류
- [x] 무이의 기간 만료 자동 정산 스케줄러
  - `Bounty.submittedAt` 컬럼 추가 (updatedAt은 다른 필드 변경에도 갱신되는
    범용 값이라, "언제 제출됐는지"를 정확히 계산하려면 전용 컬럼이 필요했음)
  - `AutoSettlementScheduler`(`@nestjs/schedule`의 `@Cron`, 매시 정각 실행)가
    `AUTO_SETTLE_DAYS`(기본 5일)를 초과한 SUBMITTED 바운티를 찾아 자동 정산
  - 정산 로직 자체는 `approve()`와 완전히 동일한 것을 재사용
    (`settleSubmittedBounty` 내부 헬퍼로 추출) — "누가 트리거했는지"만 다름
  - 관리자가 즉시 확인해볼 수 있도록 `POST /bounties/admin/auto-settle-now`
    수동 트리거 API도 추가 (ADMIN 전용)
  - `AUTO_SETTLE_DAYS=0`으로 즉시 만료시켜 수동 트리거 → 자동 정산(수수료 계산
    포함) → 일반 사용자 접근 차단까지 실제 시나리오로 검증 완료
- [ ] 배포: 프론트 Vercel, 백엔드 Render/Railway
- [ ] GitHub 저장소 연결 + 로컬 컴퓨터 폴더 동기화

## 9. 확장 기획 반영 (사용자가 직접 정리한 "API 키 사용법 + 확장판 기획서" 반영)
사용자가 직접 조사한 자료를 바탕으로 두 가지 방향이 추가되었다.

1. **PG사/오픈뱅킹 실제 샌드박스 연동**: 포트원(아임포트)·토스페이먼츠 같은 PG사는
   사업자등록 없이도 무료 테스트 API 키를 즉시 발급해준다. 즉 지금의 Mock을
   "진짜 PG사와 통신하는 샌드박스 연동"으로 바꿀 수 있다 — 나중에 사업자등록 후
   '라이브 키'로 교체만 하면 실제 상용화로 이어진다. **사용자가 포트원 계정을
   만들고 테스트 API 키를 전달하면 진행 예정** (계정 생성은 코드로 대신할 수 없음).
2. **확장판 기획의 새 아이디어들**: 마일스톤 분할 정산, 전문가 평판 점수, 동행
   서비스(부동산 임장/중고차 점검 동행 등), 안심전화번호(가상번호), AI OCR 자동
   자격증 검증, 관리자 감사 로그. 관리자 감사 로그와 자동 정산 스케줄러는 위에서
   이미 구현 완료. 나머지는 순서대로 진행 중.

### 전문가 평판(Reputation) 점수 시스템 [완료]
- `ReputationService`가 Bounty/Dispute 테이블을 실시간 집계해서 점수를 계산
  (별도 테이블에 점수를 저장하지 않음 — 항상 최신 거래 기록 기반)
- 공식: 완료율(SETTLED / (SETTLED+REFUNDED)) 70% + 분쟁 승률(RESOLVED_SETTLE
  비율) 30% 가중 평균. 완료 이력이 없으면 점수 대신 "신규 전문가"로 표시
- `GET /experts/:expertId/reputation` API 추가, 프론트 바운티 상세 화면의
  지원자 목록에 `ReputationBadge`로 노출 (의뢰인이 선택 전에 참고)
- **발견하고 고친 버그**: 이의제기를 관리자가 "환불"로 중재해도 바운티 상태가
  DISPUTED에 그대로 멈춰 있어서, "아직 처리 안 된 분쟁"과 "환불까지 끝난 분쟁"을
  구분할 수 없었다 (평판 점수의 완료율 계산 중 발견) → `BountyStatus.REFUNDED`
  종결 상태를 추가하고 `markRefundedAfterDispute()`로 명확히 전환하도록 수정
- 정상 완료 2건 + 분쟁 승리 1건 + 분쟁 패배 1건 시나리오로 완료율 75%, 점수
  67.5점이 정확히 계산되는 것을 검증

### 실제 포트원(PortOne) V2 API 결제 검증/취소 연동 [1단계 완료]
- 사용자가 admin.portone.io에서 직접 계정을 만들고(사업자등록 불필요, 테스트
  키 즉시 발급) Store ID + API Secret을 발급받아 전달 → `.env`에
  `PORTONE_STORE_ID` / `PORTONE_API_SECRET`로 저장 (`.env`는 git에 커밋되지 않음)
- `PaymentGatewayService`(추상 클래스, `StorageService`와 동일한 패턴)로 계약을
  분리: `MockPaymentGatewayService`(무조건 성공 처리, 기본값)와
  `PortOnePaymentGatewayService`(실제 `api.portone.io` REST 호출: `GET
  /payments/:id`로 결제 상태·금액 검증, `POST /payments/:id/cancel`로 실제 취소)
  두 구현체를 만들고, `PAYMENT_GATEWAY_DRIVER` 환경변수(기본 mock)로 전환한다.
- **바운티 상태 흐름 변경**: 지원자를 선택하는 즉시 LOCKED가 되던 기존 방식은
  실제 결제 없이 진행되는 것이라 실제 PG 연동과 맞지 않는다. 그래서
  `BountyStatus.PAYMENT_PENDING`(+`EscrowStatus.PENDING_PAYMENT`) 상태를
  새로 추가: 지원자 선택 → 결제 대기(paymentId 발급) → 프론트가 결제 완료를
  알려오면 서버가 `PaymentGatewayService.verifyPayment()`로 PG에 직접
  재확인한 뒤에만 진짜 LOCKED로 전환한다 (`POST /bounties/:id/confirm-payment`
  신규 API). 환불(`refundClientCancel`/`refundExpertFault`)도 이제 실제
  `cancelPayment()` 취소 요청을 거치도록 변경.
- 정산(`settleNormally`)은 여전히 내부 장부 처리로 남겨뒀다 — PG의 결제/취소
  API만으로는 전문가에게 실제 계좌 이체를 할 수 없고, 별도의 이체 시스템
  (오픈뱅킹 이체 API 또는 PG 마켓플레이스 정산 계약)이 필요한데 이건 사업자등록
  이후 단계이기 때문. 코드 주석에 이유를 명확히 남겨둠.
- **알아낸 중요한 제약**: 이 개발 환경(클라우드 샌드박스)은 보안 정책상
  `api.portone.io`로 나가는 외부 네트워크 요청을 차단한다 (조직 egress 정책 —
  npm/pypi 등 허용된 목록 외 도메인은 전부 막힘). 그래서 실제 포트원 API 호출은
  여기서 직접 테스트할 수 없고, `PAYMENT_GATEWAY_DRIVER=mock`으로 전체 플로우
  (선택→결제대기→결제확인→LOCKED→제출→승인→정산, 그리고 이의제기→환불까지)를
  curl 시나리오로 검증 완료. 실제 API 호출 검증은 배포(Render/Railway, 외부
  네트워크 제약 없음) 이후 또는 사용자 개인 컴퓨터에서 직접 실행할 때 진행 예정.
### 실제 포트원 채널 등록 + 프론트엔드 결제창 연동 [2단계 완료]
- admin.portone.io 콘솔에서 사용자가 직접 테스트 채널을 등록함: KG이니시스 +
  "결제창 일반/정기결제·API 일반/정기결제(V2)" 결제 모듈 선택 → 공용 테스트
  MID(`INIpayTest`) 선택 시 웹결제 signkey/INIAPI Key/IV가 전부 자동으로
  발급됨 (별도 PG 계약·사업자등록 불필요, 채널명 `credobounty-test`)
- 발급된 채널 키(`channel-key-...`)를 프론트엔드 `.env.local`의
  `NEXT_PUBLIC_PORTONE_CHANNEL_KEY`로 저장 (storeId와 함께 "공개용" 값이라
  브라우저에 노출돼도 안전 - API Secret과는 성격이 다름)
- `@portone/browser-sdk` 패키지 설치 후 `frontend/src/lib/portone.ts`에
  `payForBounty()` 헬퍼 작성 - `PortOne.requestPayment()`로 실제 결제창을 띄움.
  paymentId는 반드시 백엔드가 `selectApplicant()` 시점에 미리 발급해둔 값을
  그대로 써야 나중에 서버가 같은 ID로 결제를 재조회/검증할 수 있음
  (`GET /transactions/by-bounty/:id`가 이 paymentId를 프론트에 내려줌)
- 바운티 상세 화면의 결제 대기(PAYMENT_PENDING) 섹션을 `PaymentSection`
  컴포넌트로 교체: 버튼 클릭 → 실제 결제창 호출 → 성공 시 서버의
  confirm-payment 호출 → 서버가 PG 재검증 후 LOCKED 전환
- **남은 것**: 이 개발 환경(클라우드 샌드박스)의 외부 네트워크 제약 때문에
  `PAYMENT_GATEWAY_DRIVER=mock`으로 코드/흐름만 검증했고, 실제 결제창에서
  진짜 카드 테스트 결제를 끝까지 눌러보는 것과 `PAYMENT_GATEWAY_DRIVER=portone`
  전환 후 서버가 실제로 PG 응답을 검증하는 것은 배포 이후(또는 사용자 컴퓨터에서
  직접 실행할 때) 확인 예정.
  (참고: 이 프로젝트의 목적은 실제 상용 서비스 운영이 아니라 포트폴리오/취업용
  증빙이므로, 사업자등록·PG 정식 계약이 필요한 "완전한 라이브 결제"까지는
  진행하지 않기로 함 - 지금 수준의 샌드박스 검증으로 충분.)

### 마일스톤 분할 정산 구조 [완료]
- `BountyMilestone` 엔티티 추가 (bountyId, sequence, title, amount,
  status(PENDING/SUBMITTED/APPROVED), fileUrl, note, submittedAt, approvedAt)
- 전체 락업 금액(Transaction.amount)은 기존과 동일하게 지원자 선택→결제 완료
  시점에 한 번에 잠긴다 - 달라지는 건 "제출→승인"이 여러 단계로 나뉘고, 단계를
  승인할 때마다 `TransactionsService.settleMilestone()`이 그 몫만큼만 부분
  정산한다는 점. `Transaction.settledAmount`가 `amount`에 도달하는 순간에만
  `escrowStatus`가 SETTLED로 바뀐다
- `BountiesService.defineMilestones()`: 지원자를 선택하기 전(PENDING) 단계에서만
  설정 가능, 마일스톤 amount 합이 바운티 전체 금액과 정확히 일치해야 함
- `submitMilestone()`: 순서(sequence) 강제 - 이전 단계가 APPROVED 되기 전엔
  다음 단계를 제출할 수 없음
- `approveMilestone()`: 마일스톤 승인 → 부분 정산 → 마지막 마일스톤까지 전부
  APPROVED되면 바운티 전체 상태를 SETTLED로 전환
- 마일스톤이 설정된 바운티는 기존 `submitResult()`/`approve()`(일반 흐름) 사용을
  막고 `/milestones/:milestoneId/submit`·`/approve`를 쓰도록 안내 에러 반환
- **알려진 제한사항**: 마일스톤 바운티에 대한 이의제기(dispute) 흐름은 아직
  마일스톤 단위로 지원하지 않음 (일반 바운티의 SUBMITTED 상태 기반 흐름이라서) -
  현재 범위 밖으로 남겨둠
- 3단계(각 10만원, 총 30만원)로 나눈 바운티로 시나리오 테스트: 순서 위반 제출
  차단 확인 → 1단계 제출/승인(부분 정산, LOCKED 유지) → 2단계 → 3단계 제출/승인
  → 바운티 전체 SETTLED 전환, settledAmount 300000/platformFeeAmount 30000
  정확히 누적되는 것까지 확인 완료

## 10. 다음 단계 우선순위 (2026-09-14 기준)
이 프로젝트는 실제 상용 서비스 운영이 아니라 포트폴리오/취업용 증빙이 목적이므로,
사업자등록이 필요한 완전한 라이브 결제 연동까지는 진행하지 않는다. 남은 작업은:
1. 동행 서비스(부동산 임장/중고차 점검 동행 등) 도메인 확장
2. 안심전화번호(가상번호) Mock 서비스
3. AI OCR 자동 자격증 검증 Mock 서비스
4. GitHub 저장소 연결 + 로컬 컴퓨터 폴더 동기화
5. 배포 (프론트 Vercel, 백엔드 Render/Railway) - 포트폴리오/데모 링크 공유 목적

## 11. 동행 서비스 (부동산 임장/중고차 점검 등 현장 동행) [완료]

### 배경 (기획서 확장 4장)
바운티 중 일부는 "원격으로 파일만 주고받고 끝나는 일"이 아니라, 전문가가
실제 현장에 함께 가야 하는 일이다 (부동산 매매 전 임장 동행, 중고차 구매 전
정비사 동행 점검 등). 이런 건은 "예약된 시간/장소"라는 개념이 추가로 필요하고,
"그 시간이 되기 전에는 결과물을 제출할 수 없다"는 제약도 걸어야 한다.

### 설계 결정
새로운 도메인(DomainType)을 추가하는 대신, 기존 15개 도메인 중 아무거나에
붙을 수 있는 "진행 방식" 축으로 별도 모델링했다 (`ServiceType` enum:
REMOTE/COMPANION). 부동산 권리분석 도메인이 원격으로 서류만 검토할 수도 있고,
현장 임장 동행일 수도 있기 때문에 — "무슨 일이냐"와 "어떻게 진행하냐"는
서로 다른 축이라고 판단했다.

### 구현
- `Bounty` 엔티티에 `serviceType`(기본값 REMOTE), `scheduledAt`, `location`
  컬럼 추가
- `BountiesService.create()`: COMPANION 선택 시 `scheduledAt`+`location`
  필수, `scheduledAt`은 반드시 현재보다 미래여야 함 (검증 실패 시 400 에러)
- `BountiesService.submitResult()`: COMPANION 바운티는 예약된 시간이 되기
  전에는 결과물 제출이 막힘 ("아직 예약된 동행 일시가 되지 않았습니다" 에러)
- 프론트엔드 바운티 등록 폼(`/bounties/new`)에 "진행 방식" 선택 드롭다운
  추가, COMPANION 선택 시에만 예약 일시/장소 입력란이 나타남
- 바운티 상세 페이지에 COMPANION 바운티일 경우 "현장 동행 · 일시 · 장소"
  안내 박스 표시

### 테스트 결과 (curl 시나리오, 전체 통과)
1. COMPANION인데 장소 누락 → 400 에러 확인
2. COMPANION인데 과거 날짜 → 400 에러 확인
3. 정상 COMPANION 바운티 생성 → 성공
4. 지원 → 선택 → 결제 확인까지 정상 진행
5. 예약 일시(미래) 전에 결과물 제출 시도 → 차단 확인
6. 회귀 테스트: REMOTE(기본값, serviceType 생략) 바운티는 기존과 동일하게
   즉시 제출 가능한지 확인 → 정상

### 사소한 버그 수정
`Bounty.location` 컬럼을 `@Column({ nullable: true })`로만 선언했다가
TypeORM이 `string | null` 타입을 추론하지 못해 `DataTypeNotSupportedError`
발생 — `type: 'varchar'`를 명시해서 해결 (이 프로젝트에서 반복적으로 나오는
TypeORM nullable 컬럼 패턴이라 다음에도 주의).

## 12. 안심전화번호(가상번호) Mock 서비스 [완료]

### 배경
배달앱/중고거래 플랫폼의 "안심번호"와 같은 개념. 의뢰인과 전문가가 매칭된 뒤
서로 연락해야 할 일이 생기는데(특히 동행 서비스처럼 약속을 잡아야 하는 경우),
실제 전화번호를 직접 주고받게 하면 개인정보 노출 위험이 있다. 가상번호 하나를
통해서만 연결되게 해서 이 위험을 없앤다.

### 설계 결정
- 다른 Mock 서비스(StorageService/PaymentGatewayService)와 달리 인터페이스로
  분리하지 않고 `MockSafeNumberService` 하나로 끝냈다 - 안심번호는 통신사 전용
  회선/API 계약이 있어야만 실제로 붙여볼 수 있는 영역이라, 이 프로젝트 범위
  (취업용 포트폴리오, 실제 상용 서비스 아님)에서는 Mock 개념 증명까지만 하는 게
  맞다고 판단
- `SafeNumberMapping` 엔티티로 바운티(매칭) 하나당 가상번호 하나를 발급/기록.
  "지금 이 번호가 살아있는지"는 별도 활성화 플래그 없이, 조회 시점에 바운티
  상태(LOCKED~DISPUTED)를 같이 확인하는 방식으로 단순화
- User 엔티티에 `phoneNumber` 컬럼 추가 (회원가입 시 선택 입력, 또는
  `PATCH /users/me/phone`으로 나중에 등록/수정 가능) - 상대방에게는 이 값이
  절대 그대로 노출되지 않고, 가상번호를 통해서만 간접 연결됨

### API
- `GET /bounties/:id/safe-number` - 발급된 번호 조회 (없으면 404)
- `POST /bounties/:id/safe-number` - 최초 발급 또는 기존 번호 반환(멱등)
- `POST /bounties/:id/safe-number/call` - 안심번호로 "통화 연결" 흉내 (Mock)
- `PATCH /users/me/phone` - 내 전화번호 등록/수정

### 검증 규칙
- 바운티 상태가 LOCKED/SUBMITTED/DISPUTED(매칭~종료 전)일 때만 발급 가능
- 의뢰인 본인 또는 매칭된 전문가 본인만 접근 가능 (제3자 403)
- 양쪽 다 전화번호를 등록해야 발급 가능 - 누가 안 했는지 에러 메시지에 명시
- 프론트엔드는 전화번호 미등록 에러를 감지하면 바로 그 화면에서 번호를
  등록하고 재시도할 수 있는 인라인 폼을 보여줌

### 테스트 결과 (curl 시나리오, 전체 통과)
1. 전화번호 없이 회원가입 (선택 입력이라 성공)
2. 전화번호 형식 오류 회원가입 시도 → 400 차단
3. PENDING(매칭 전) 상태에서 발급 시도 → 차단
4. 전화번호 미등록 상태에서 발급 시도 → "의뢰인이(가) 아직 전화번호를
   등록하지 않아..." 에러로 어느 쪽이 문제인지 명시
5. 관계없는 제3자 발급 시도 → 403 차단
6. 전화번호 등록 후 발급 → 성공, `050-XXXX-XXXX` 형식 가상번호 반환
7. GET 재조회 및 POST 재호출 모두 같은 번호 반환 (멱등성 확인)
8. Mock 통화 연결 → 성공 로그 반환

### UX 문구 관련 (기획 논의 기록)
바운티 등록 화면의 "진행 방식" 옵션 라벨을 "원격 작업/현장 동행"에서
"비대면 진행/현장 동행"으로 변경 - 사용자가 "원격 작업"이 어색하다고 지적,
"전문가지원/전문가 동행"을 제안했으나 기존 "지원하기"(전문가가 바운티에
지원하는 기능) 버튼과 의미가 겹쳐 혼동 우려가 있어 "비대면 진행"으로 대체.

## 13. AI OCR 자동 자격증 검증 Mock 서비스 [완료]

### 배경
기존에는 `MockVerificationService`가 "자격증 번호 글자 수가 4자 이상이면 통과"
라는 느슨한 형식 검사만 했다. 실제 서비스에서는 여기에 한 단계를 더 얹는다 -
증빙 서류(자격증 사진/PDF)를 OCR로 읽어서, 서류에 실제로 적힌 내용이 입력한
자격증 번호와 일치하는지까지 자동으로 대조하는 것.

### 3단계 자동 심사 흐름
1. 형식 검증(`MockVerificationService`, 기존과 동일) - 형식이 이상하면 즉시 REJECTED
2. 증빙 서류를 첨부하지 않았다면 형식 검증만으로 APPROVED (자동 대조 생략됐다는
   사실을 reviewNote에 명시)
3. 증빙 서류를 첨부했다면 `MockOcrService`가 "서류에서 추출한 글자가 자격증
   번호와 일치하는지" 대조 - 일치하면 APPROVED, 불일치하면 자동 승인하지 않고
   PENDING(관리자 수동 검토 대기)으로 남긴다

### Mock OCR 구현 방식
실제로 이미지를 읽을 수는 없으니, 파일 내용의 SHA-256 해시를 시드로 써서
"OCR이 이 파일을 읽었을 때 이 정도 확률로 성공했을 것"이라는 시나리오를
재현 가능하게(같은 파일 = 항상 같은 결과) 흉내냈다. 인식 성공률을 85%로 설정해서
"가끔 실패해서 사람이 검토해야 하는 경우"까지 재현했다 - 무조건 성공보다 현실적인
Mock이라고 판단. 실제 전환 시 Upstage Document AI, Naver Clova OCR, Google Cloud
Vision 등으로 교체 가능하도록 반환 형태(matched/confidenceScore)는 고정해뒀다.

### 관리자 수동 검토 (신규)
- `VerificationStatus.PENDING`을 처음으로 실제 사용 - 기존에는 정의만 되어있고
  쓰이지 않았음
- `GET /certifications/pending` - AI가 보류한 신청 대기열 (관리자 전용)
- `POST /certifications/:id/review` - 최종 승인/반려 (관리자 전용, 관리자 감사
  로그에 기록됨 - 기존 DisputesController.resolve와 동일한 패턴 재사용)
- 이미 처리된 건은 재검토 불가하도록 막음

### 프론트엔드
- 인증 신청 화면(`/certifications`)에 증빙 서류 첨부 필드 추가 (선택 입력)
- 안내 문구를 "AI OCR이 자동으로 대조하며, 애매하면 관리자가 검토할 때까지
  '심사중' 상태로 남는다"는 내용으로 업데이트
- 관리자 검토 화면은 별도로 만들지 않음 - 기존 이의제기 중재(disputes resolve)와
  동일하게 API 레벨에서만 제공 (프론트 관리자 페이지가 없는 기존 패턴을 그대로 따름)

### 테스트 결과 (curl 시나리오, 전체 통과)
1. 증빙 서류 없이 신청 → 형식 검증만으로 즉시 APPROVED
2. 증빙 서류 첨부, OCR "일치" 시나리오 파일 → APPROVED (신뢰도 %와 함께)
3. 증빙 서류 첨부, OCR "불일치" 시나리오 파일 → PENDING(심사중)으로 보류
4. 일반 사용자가 관리자 전용 API(`/pending`, `/review`) 접근 시도 → 403 차단
5. 관리자가 PENDING 건 승인 처리 → APPROVED로 전환, 감사 로그 기록
6. 이미 처리된 건 재검토 시도 → 400 차단
7. PENDING 상태 인증만 있는 전문가가 해당 도메인 바운티에 지원 시도 → 기존
   `hasApprovedCertification` 로직(APPROVED만 인정)이 코드 변경 없이 정상 차단하는
   것까지 확인

## 14. 로그인/회원가입 Rate Limiting [완료]

### 배경
로그인/회원가입 API에 요청 횟수 제한이 전혀 없었다 - 봇이 비밀번호를 무차별
대입(brute force)하거나, 이메일을 계속 바꿔가며 계정을 대량 생성하는 것을
막을 방법이 없었다.

### 구현
`@nestjs/throttler` 도입 (v6, `@Throttle({ default: { limit, ttl } })` API).
- 전역 기본값: IP 하나당 1분에 60번 (`AppModule`에 `APP_GUARD`로 전역 적용)
- 다음 4개 엔드포인트는 전역 기본값보다 훨씬 엄격한 "1분에 5번"으로 덮어씀:
  `POST /auth/register`, `POST /auth/login`, `POST /auth/forgot-password`,
  `POST /auth/reset-password` - 전부 브루트포스/이메일 대량 생성/메일 폭탄
  공격의 표적이 되기 쉬운 엔드포인트들

### 테스트 결과
- 로그인 실패(잘못된 비밀번호)를 같은 IP에서 8번 연속 시도 → 1~5번째는 401,
  6번째부터 429(Too Many Requests)로 정상 차단
- 회원가입도 동일하게 6번째 시도부터 429 확인
- 일반 조회 API(`GET /bounties`)는 전역 제한(60/분) 범위 내에서 영향 없이
  정상 동작하는 것 확인

### 참고
`npm install` 이후 `npm audit`에서 취약점 개수가 늘어난 것처럼 보이지만,
전부 `@nestjs/cli`, `@angular-devkit/*`, `webpack`, `tar` 등 기존에 이미
있던 개발 도구용 의존성 체인에서 나온 것이고, 이번에 추가한
`@nestjs/throttler` 자체에서 나온 취약점은 없다 (npm audit --json으로 직접
확인). 전부 devDependency이고 실제 운영 서버 실행에는 포함되지 않는다.

## 15. 다단계 작업 DB 트랜잭션 처리 [완료]

### 배경
지원자 선택, 결제 확인, 마일스톤 승인, 이의제기 중재 같은 흐름은 서로 다른
테이블(Bounty/BountyApplication/BountyMilestone와 Transaction)에 걸쳐
여러 번 쓰기가 일어난다. 지금까지는 이 쓰기들이 하나로 묶여있지 않아서, 중간에
서버가 죽거나 한쪽 쿼리만 실패하면 "결제는 확인됐는데 바운티는 여전히 결제
대기중", "환불은 됐는데 바운티 상태는 안 바뀜" 같은 불일치가 이론적으로
가능했다 (실제로 이전 세션에서 발견했던 REFUNDED 상태 버그도 이런 종류의
문제였다 - 그때는 코드 로직 누락이었지만, 트랜잭션이 없으면 같은 종류의 문제가
인프라 장애로도 발생할 수 있다).

### 구현 방식
TypeORM의 `DataSource.transaction()`을 사용했다 - 내부적으로 QueryRunner
하나를 열어(connect → startTransaction) 콜백 안의 모든 쿼리를 같은 커넥션으로
실행하고, 콜백이 정상 종료되면 commit, 예외가 던져지면 자동으로 rollback한 뒤
반드시 release까지 해준다. 수동으로 QueryRunner를 만들어 commit/rollback/
release를 직접 챙기는 것과 동작은 동일하지만, release를 깜빡하는 등의 실수를
원천 차단해주는 더 안전한 방식이라 이쪽을 선택했다.

여러 서비스(BountiesService/TransactionsService/DisputesService)가 각자
자기 테이블의 리포지토리를 갖고 있어서, 트랜잭션을 하나로 묶으려면 트랜잭션을
시작한 쪽의 EntityManager를 끝까지 전달해야 한다. 그래서 관련 메서드들에
선택적 마지막 인자 `manager?: EntityManager`를 추가했다 - 넘겨받으면 그
매니저로 쿼리해서 같은 트랜잭션에 합류하고, 안 넘겨받으면 기존처럼 독립적으로
즉시 커밋된다 (하위 호환).

### 트랜잭션으로 묶은 흐름
- `selectApplicant`: 지원자 상태 일괄 변경 + 바운티 상태 전환 + 결제 대기
  트랜잭션 생성
- `confirmPayment`: PG 결제 확인 + 바운티 LOCKED 전환
- `approve`/`autoSettleExpired`(공유 로직 `settleSubmittedBounty`): 정산
  처리 + 바운티 SETTLED 전환
- `approveMilestone`: 부분 정산 + 마일스톤 APPROVED 전환 + (마지막 단계면)
  바운티 SETTLED 전환
- `DisputesService.file`: 바운티 DISPUTED 전환 + 자금 FROZEN 동결 + 분쟁
  기록 생성
- `DisputesService.resolve`: 환불/정산 처리 + 바운티 상태 전환(REFUNDED/
  SETTLED) + 분쟁 기록 업데이트. 관리자 감사 로그(auditService.record)는
  의도적으로 트랜잭션 밖에 남겨뒀다 - AuditService 자체 설계 원칙("로그 기록
  실패가 원래 하려던 작업을 막으면 안 된다")과 일치시키기 위함

### 테스트 결과 (curl 회귀 시나리오, 전체 통과)
기존 핵심 흐름 4가지를 처음부터 끝까지 다시 돌려서 트랜잭션 도입으로 로직이
깨지지 않았는지 확인:
1. 일반 흐름(지원→선택→결제→제출→승인) → SETTLED, settledAmount=바운티
   금액과 정확히 일치
2. 마일스톤 분할 정산(2단계) → 1단계 승인 후 LOCKED 유지 + 부분 정산,
   2단계(마지막) 승인 후 바운티/거래 둘 다 SETTLED로 동시 전환
3. 이의제기 환불(refund=true) → 바운티/거래 둘 다 REFUNDED로 동시 전환
   (예전 버그가 재발하지 않는지 재확인)
4. 이의제기 정산(refund=false) → 바운티/거래 둘 다 SETTLED로 동시 전환,
   관리자 감사 로그에도 두 건 다 정상 기록

## 16. 바운티 목록 페이지네이션 [완료]

### 구현
`GET /bounties`가 이제 배열 대신 `{ items, total, page, limit, totalPages }`
형태로 응답한다. offset 기반(page/limit) 방식을 선택 - 이 프로젝트 규모에서는
커서 기반보다 구현과 설명이 단순해서 더 적합하다고 판단. `limit`은 클라이언트가
과도하게 큰 값을 보내 한 번에 전체를 긁어가지 못하도록 최대 50으로 강제한다.
기본값은 page=1, limit=12(백엔드 기본값) / 프론트엔드는 페이지당 10개로 호출.

프론트엔드 바운티 목록 화면에 "이전/다음" 버튼 + "N / 총페이지 페이지 (전체
M건)" 표시를 추가했다.

### 테스트 결과
- page/limit 생략 시 기본값으로 정상 응답
- limit=2로 1페이지, 2페이지를 각각 조회 → 서로 다른 항목이 반환되는 것 확인
- limit=200 요청 → 실제로는 50으로 강제되는 것 확인

## 17. Swagger/OpenAPI 문서 자동화 [완료]

### 구현
`@nestjs/swagger`를 도입해 `/api-docs`에서 전체 API 문서를 자동 생성하고,
브라우저에서 "Try it out"으로 바로 호출까지 해볼 수 있게 했다.
(주의: `@nestjs/swagger` 최신 버전은 NestJS v12를 요구해서 이 프로젝트의
NestJS v10과 호환되지 않는다 - `@nestjs/swagger@^7`로 설치해 버전을 맞췄다.)

- `main.ts`에 `DocumentBuilder`로 제목/설명/버전 + JWT Bearer 인증 스킴을
  등록, `SwaggerModule.setup('api-docs', ...)`으로 문서 화면 노출
- 컨트롤러 9개 전부에 `@ApiTags(...)`를 붙여 태그별(health/users/auth/
  bounties/certifications/disputes/transactions/reputation/admin)로 묶었고,
  인증이 필요한 컨트롤러/엔드포인트에는 `@ApiBearerAuth('access-token')`을
  붙여서 화면에 자물쇠 아이콘이 표시되고 "Authorize" 버튼으로 토큰을 넣으면
  바로 호출 테스트가 가능하도록 했다

### 테스트 결과
- `/api-docs` 정상 응답(HTTP 200), Playwright로 실제 렌더링 스크린샷까지 확인
- `/api-docs-json`(OpenAPI 스펙)을 파싱해 총 33개 경로, 9개 태그 전부 정상
  등록된 것, `securitySchemes`(JWT Bearer)가 스펙에 포함된 것 확인

## 18. 포트원(PortOne) 웹훅 수신 엔드포인트 [완료]

### 왜 필요한가
지금까지 결제 확인(LOCKED 전환)은 프론트엔드가 "결제 끝났어요"라고 알려주는
시점에만 서버가 PG에 재확인하는 구조였다. 사용자가 결제 직후 창을 닫아버리거나
네트워크가 끊기면 이 통보가 서버에 영영 안 올 수 있다. 웹훅은 PG(포트원)가
"그 결제 실제로 어떻게 됐는지"를 사용자 브라우저를 거치지 않고 서버끼리 직접
알려주는 통로라서, 이런 누락을 백업으로 잡아줄 수 있다.

### 사전 조사 (구현 전 공식 문서 확인)
포트원 V2 웹훅 문서(`developers.portone.io/opi/ko/integration/webhook/readme-v2`)를
확인한 결과, 포트원은 자체 규격이 아니라 [Standard Webhooks](https://www.standardwebhooks.com/)
스펙(HMAC-SHA256 기반 서명 검증)을 그대로 따르고, 직접 구현 대신 공식 SDK
(`@portone/server-sdk`)의 `Webhook.verify(secret, payload, headers)` 사용을
권장하고 있었다. 재시도 정책은 실패 시 최대 5회, 0→1→4→16→64→256분 간격
(jitter 적용) - 그래서 같은 이벤트가 중복으로 올 수 있다는 전제로 멱등하게
설계해야 한다.

### 구현
- `@portone/server-sdk` 설치 후 `Webhook.verify()`로 서명을 검증하는
  `WebhooksService`/`WebhooksController`(`POST /api/webhooks/portone`) 추가.
  로그인 사용자가 아니라 PG가 호출하는 엔드포인트라 JWT 인증은 없고, 대신
  서명 검증만 통과한 요청만 처리한다 (서명 검증 실패 시 401).
- 서명 검증은 "가공 전 원문 body 문자열" 기준이라, `main.ts`에서
  `NestFactory.create(AppModule, { rawBody: true })`로 바꿔서 express가
  파싱하기 전 원본 바이트(`req.rawBody`)를 그대로 받도록 했다.
- 검증에 성공한 이벤트는 전부 `payment_webhook_logs` 테이블(신규, append-only)에
  원문과 함께 남긴다 - 나중에 "그 웹훅이 실제로 왔었는지" 확인할 근거가 된다.
  (기존 `AdminAuditLog`는 "관리자가 한 조치" 전용이라 admin_id가 필수라서,
  PG가 보낸 이벤트 기록에는 안 맞아 별도 테이블로 분리했다.)
- 결제 완료(`Transaction.Paid`) 이벤트를 받으면, 해당 paymentId의 거래가 아직
  `PENDING_PAYMENT`(결제 대기)면 `BountiesService.confirmPayment()`를 호출해
  자동으로 `LOCKED`까지 확정한다. **처음에는 `TransactionsService.confirmLock()`을
  직접 불렀다가, 테스트 중 Transaction의 `escrowStatus`만 LOCKED로 바뀌고
  Bounty 자체의 `status`는 여전히 `PAYMENT_PENDING`으로 남는 불일치를 발견해서
  `BountiesService.confirmPayment()`(Task #26에서 둘을 하나의 DB 트랜잭션으로
  묶어둔 메서드)를 쓰도록 고쳤다** - 아래 테스트 기록 참고.
- 취소/부분취소 이벤트는 로그만 남기고 자동 환불 처리는 하지 않는다 - 우리
  시스템은 환불 사유(단순변심/전문가귀책/이의제기)에 따라 수수료 계산이
  달라지는데, 웹훅만으로는 어느 사유인지 알 수 없어서 섣불리 자동화하면
  이중 환불 같은 더 위험한 버그로 이어질 수 있다고 판단했다 (의도적으로
  범위를 좁힌 부분 - 향후 과제로 남김).
- 같은 이벤트가 중복 도착해도(재시도 정책) 이미 LOCKED인 거래는 조용히
  건너뛰어 멱등하게 처리한다.

### 이 샌드박스의 한계와, 그럼에도 실제로 검증한 방법
이 환경은 우리가 바깥(`api.portone.io`)으로 나가는 요청은 막혀 있지만, 반대로
외부에서 우리 서버로 "들어오는" 요청을 받는 것 자체는 막혀있지 않다. 그래서
실제 포트원 서버가 보내는 웹훅은 배포 후에나 받아볼 수 있지만, 서명 검증은
대칭키(HMAC) 방식이라 우리가 같은 시크릿으로 직접 서명한 요청을 만들면
로컬에서도 진짜와 동일한 방식으로 완전히 검증할 수 있다. SDK 소스 코드를
직접 읽어 서명 알고리즘(`HMAC-SHA256(secret, "{id}.{timestamp}.{payload}")`,
`v1,<base64>` 형식)을 정확히 재현한 테스트 스크립트로 검증했다.

### 테스트 결과 (실제 바운티 생성 → 지원 → 선정까지 마친 뒤 웹훅 전송)
1. 정상 서명 + 실존하는 paymentId → 200 응답, `payment_webhook_logs`에 기록,
   해당 거래의 `escrowStatus`가 `LOCKED`로, **바운티 자체의 `status`도
   `LOCKED`로** 정확히 전환됨을 DB에서 직접 확인 (위에서 발견한 버그 수정 후 재검증)
2. 서명 위조(시그니처 값을 조작) → 401 거부
3. 페이로드 변조(서명은 원본 그대로 두고 본문만 바꿔치기) → 401 거부
   (서명이 본문 내용까지 포함해서 계산되므로 본문만 바꾸면 반드시 깨진다)
4. 필수 헤더(`webhook-signature`) 누락 → 401 거부
5. 존재하지 않는 paymentId로 정상 서명된 요청 → 200 응답(웹훅 자체는 정상
   수신했다고 PG에 알려줘야 재전송 폭탄을 피할 수 있음), 서버 로그에는
   경고만 남기고 조용히 무시 - 에러로 죽지 않음을 확인
6. 이미 LOCKED된 거래에 같은 웹훅을 다시 보내도(중복 전송 시나리오) 에러 없이
   멱등하게 무시됨을 확인

## 19. 인앱 알림 로그 시스템 [완료]

### 구현
실시간 푸시(웹소켓)가 아니라 "쌓여있는 알림 목록"을 화면에서 확인하는
방식으로 범위를 잡았다 - 이 프로젝트 규모에서 웹소켓 인프라를 새로 두는 것보다
훨씬 단순하면서, "상대방이 뭔가 했다"를 놓치지 않게 해준다는 목적은 동일하게
달성한다.

- `Notification` 엔티티(신규, `notifications` 테이블) + `NotificationsService`
  (`notify/findMine/countUnread/markRead/markAllRead`) + `NotificationsController`
  (`GET /notifications/me`, `GET /notifications/me/unread-count`,
  `PATCH /notifications/:id/read`, `PATCH /notifications/read-all`)
- AuditService와 같은 원칙: 알림 생성 실패가 원래 하려던 핵심 작업(지원자 선택,
  결제 확인 등)을 막으면 안 된다. 그래서 `notify()`는 항상 DB 트랜잭션이 커밋된
  "뒤에" 별도로 호출하고, 실패해도 예외를 던지지 않고 로그만 남긴다.
- 알림이 발생하는 지점 (BountiesService/DisputesService/CertificationsService):
  지원자 선택(선택된 전문가 + 반려된 나머지 지원자 전원), 결제 확인(전문가),
  결과물 제출(의뢰인, 일반/마일스톤 둘 다), 정산 완료(전문가, approve()와
  자동정산 스케줄러가 공유하는 헬퍼 하나에만 붙여서 두 경로 다 커버), 마일스톤
  승인(전문가), 이의제기 접수(상대방인 전문가), 이의제기 처리 결과(의뢰인+전문가
  양쪽), 자격 인증 승인/반려(신청한 전문가). Task #29에서 만든 포트원 웹훅의
  자동 lock 확정도 내부적으로 `BountiesService.confirmPayment`를 그대로
  재사용하기 때문에 알림도 자동으로 따라온다.
- 프론트엔드: 로그인 상태에서만 보이는 알림 벨(`NotificationBell.tsx`)을
  Navbar에 추가. 30초 간격으로 "안 읽은 개수"만 가볍게 폴링하고, 벨을 클릭했을
  때만 실제 목록을 불러온다. 알림을 클릭하면 읽음 처리 후 관련 바운티 상세
  페이지로 이동한다.

### 테스트 결과 (실제 바운티로 전체 플로우 실행)
지원 선택 → 결제 확인 → 결과물 제출 → 승인(정산)까지 실제 API를 순서대로
호출하면서 각 단계마다 수신자의 알림이 정확히 쌓이는지 확인:
1. 지원자 선택 직후 → 선택된 전문가에게 `APPLICATION_SELECTED` 알림 생성 확인
2. 결제 확인 직후 → 전문가에게 `PAYMENT_LOCKED` 알림 추가 확인 (총 2건)
3. 결과물 제출 직후 → 의뢰인에게 `SUBMISSION_RECEIVED` 알림 확인
4. 승인(정산) 직후 → 전문가에게 `BOUNTY_SETTLED` 알림 추가 확인 (총 3건)
5. 알림 하나 읽음 처리 → unread-count가 정확히 1 감소하는 것 확인
6. 전체 읽음 처리 → unread-count가 0이 되는 것 확인
7. 다른 사람의 알림을 읽음 처리 시도 → 403 Forbidden으로 정상 차단 확인
   (알림도 본인 것만 조작 가능해야 하는 개인정보이므로 소유권 검사 필수)

## 20. 보안 강화 1탄 - helmet / JWT_SECRET / 의존성 취약점 정리 [완료]

사용자가 지금까지 만든 기능을 쭉 훑어보고 "보안 관점에서 더 강화할 부분이
있는지" 물어봐서, 실제 코드를 다시 점검해 발견한 것들을 하나씩 처리했다.

### 1) helmet 보안 헤더 적용
`main.ts`에 `app.use(helmet({ contentSecurityPolicy: false }))` 추가.
X-Content-Type-Options, X-Frame-Options(클릭재킹 방지), HSTS 등 기본 보안
헤더가 모든 응답에 자동으로 붙는다. CSP만 꺼둔 이유: 이 서버가 `/api-docs`
(Swagger UI, 인라인 스크립트 사용)도 같이 서빙하는데 기본 CSP를 켜면 그
화면이 깨진다. 이 서버는 실제 사용자 화면을 직접 서빙하지 않으므로(프론트는
별도 Next.js 서버) CSP 없이도 다른 헤더들만으로 실질적 방어 효과는 충분.

### 2) JWT_SECRET 하드코딩 기본값 폴백 제거
기존에는 `process.env.JWT_SECRET ?? '하드코딩된값'` 형태로, 배포 시
환경변수를 깜빡 채우지 않아도 서버가 "조용히" 그 고정 문자열로 떠버리는
문제가 있었다 - 공개 소스코드에 그 문자열이 그대로 있으니 누구나 유효한
JWT를 위조할 수 있는 상태가 된다.

`src/common/config/jwt-secret.util.ts`의 `getRequiredJwtSecret()`을 만들어
`JwtModule`(auth.module.ts)과 `JwtStrategy` 양쪽이 반드시 이 함수 하나로만
시크릿을 가져오게 통일했다. 이 함수는:
- 환경변수가 아예 없으면 → 예외를 던져 서버 부팅을 막는다
- `.env.example`에 있는 플레이스홀더 값("change-me-to-a-long-random-string")
  그대로면 → 역시 부팅을 막는다 (이 문자열도 공개 코드에 있으니 실사용 금지)
- 32자 미만으로 너무 짧으면 → 역시 부팅을 막는다

"조용히 약하게 뜨는 것"보다 "아예 안 뜨는 것"을 택한 fail-fast 설계다.
로컬 `.env`는 `openssl rand -hex 32`로 새로 생성한 64자 무작위 값으로 교체했다.

**테스트**: (1) JWT_SECRET을 플레이스홀더 값으로 강제한 상태로 기동 → 부팅
자체가 예외로 즉시 실패하는 것 확인 (2) 아예 빈 값으로 기동 → 마찬가지로
즉시 실패 확인 (3) 정상 값으로 재기동 → 정상 기동 확인, 이 과정에서 시크릿이
바뀌었으니 이전에 발급된 JWT들은 전부 401로 무효화되는 것까지 확인(의도된
정상 동작 - 실제로 시크릿이 바뀌었다는 증거).

### 3) 의존성 취약점(`npm audit`) 정리
- **critical 1건 해결**: `bcrypt` 5.x → 6.x 업그레이드 (기존 취약점은
  `@mapbox/node-pre-gyp`가 물고 있던 `tar` 패키지 문제 - 네이티브 바이너리
  설치 시에만 쓰이는 빌드 도구라 런타임 노출은 아니었지만, 패치가 나와있어
  그대로 반영). bcrypt.hash/compare 실제 동작(회원가입→로그인, 틀린 비밀번호
  거부)까지 재테스트해서 API가 안 바뀐 것 확인.
- **high 1건 해결 (파일 업로드 관련이라 특히 중요)**: `multer`를 1.4.5→2.3.0로
  올렸는데, `@nestjs/platform-express`가 자기 자신의 `node_modules` 안에
  `multer@2.0.2`를 **정확히 고정된 버전**으로 따로 갖고 있어서(우리
  `package.json`을 올려도 `FileInterceptor` 내부는 여전히 취약한 2.0.2를
  쓰는 상태였다), `package.json`에 `"overrides": { "multer": "^2.3.0" }`를
  추가해 의존성 트리 전체(중첩된 사본 포함)가 패치된 버전 하나로 강제
  통일되게 했다. 실제로 인증서 파일을 업로드해봐서 `FileInterceptor`가
  여전히 정상 동작하는 것까지 확인.
- **남겨둔 것 (근거 명시)**: `@nestjs/cli`가 물고 있는 webpack/glob/ajv 등
  다수는 전부 **개발 도구 전용**(`nest build`가 기본적으로 쓰는 게 아니라
  `--webpack` 옵션을 켰을 때만 관여, 코드 생성기 등) 이라 배포된 서버
  런타임에는 포함되지 않는다. 이걸 완전히 없애려면 `@nestjs/*` 전체를
  v10→v12로 올려야 하는데, 이는 Swagger 버전 호환성 때부터 계속 "별도
  마이그레이션 범위"로 미뤄온 결정과 같은 이유로 이번에도 범위 밖으로 남겼다.
- **프론트엔드(`next`)에서 critical 2건 발견, 이번엔 업그레이드하지 않기로
  판단**: Next.js 14.2.35에 RCE/SSRF급 취약점들이 보고되어 있고 공식 수정은
  16.x 메이저 업그레이드뿐이다. 다만 실제 코드를 확인해보니 이 취약점들이
  걸려있는 기능(`next/image`, Server Actions, `middleware.ts`, i18n 설정)을
  **이 프로젝트가 하나도 쓰지 않는다**(직접 grep으로 확인). 유일하게 걸리는
  게 `next.config.js`의 `rewrites()`인데, 목적지 주소가 사용자 입력이 아니라
  고정된 백엔드 환경변수라 "공격자가 목적지를 조작"하는 시나리오 자체가
  성립하지 않는다. 14→16은 사이에 15 전체를 건너뛰는 큰 점프라 이 늦은
  시점에 블라인드로 올렸다가 빌드/런타임이 깨질 위험이, 지금 낮게 평가한
  실제 노출 위험보다 크다고 판단해 미뤘다. **배포(Task #35) 전에 별도
  회귀 테스트와 함께 정식으로 업그레이드할 것을 권장** - 지금은 "안전하다"가
  아니라 "이 프로젝트 구성에서는 노출 경로가 없다"는 것까지만 확인된 상태.

## 21. 보안 강화 2탄 - 업로드 파일 매직바이트 검증 + multer 용량 제한 [완료]

### 배경
Task #37/#38/#42(보안 강화 1탄)에서 helmet, JWT_SECRET 강화, npm 취약점
정리를 마친 뒤, 사용자가 업로드 검증 흐름을 직접 지적했다: "용량 체크가
'다 읽고 나서' 이뤄져서, 아주 큰 파일을 고의로 보내면 메모리 부담이 생길
여지가 있다"는 지적과 함께, 파일 크기 상한(당시 50MB 단일값)이 너무
넉넉한 것 아니냐는 질문이 이어졌다. 논의 끝에:
- 용도별로 다른 상한을 두기로 함 (자격 인증 증빙서류 vs 바운티 결과 파일)
- 자격 인증 증빙서류: **15MB** (여러 페이지 PDF 스캔본 감안)
- 바운티 결과 파일: **20MB** (코드 zip, 진단서 PDF 등 조금 더 클 수 있음)
- "AI OCR이 먼저 확인하니 용량을 올려도 되지 않냐"는 아이디어에 대해서는,
  OCR Mock은 증빙 서류의 자격증 번호 "문자열 일치"만 대조할 뿐 악성 콘텐츠
  여부는 전혀 검사하지 않는다는 점을 확인해 드리고, 안전장치로 세지 않기로
  정리함.

### 1) 매직바이트(파일 시그니처) 검증 추가
기존에는 파일 확장자만 보고 통과시켰다 - 확장자는 파일 이름의 일부일
뿐이라 `.exe`를 `.png`로 바꿔 올리는 식의 위장을 막지 못했다. 이제
확장자 검사를 통과한 뒤에도 실제 파일 내용 맨 앞 바이트가 그 포맷의
진짜 시그니처와 일치하는지 추가로 대조한다.

- PDF: `%PDF` (`25504446`)
- PNG: `89504e470d0a1a0a`
- JPEG/JPG: `ffd8ff`
- ZIP: `PK\x03\x04`(로컬 파일 헤더) / `PK\x05\x06`(빈 zip) / `PK\x07\x08`(분할
  압축) 세 가지 시그니처 모두 허용

불일치 시 `파일 내용이 .pdf 형식과 일치하지 않습니다 (확장자만 바꾼
파일일 수 있습니다)` 같은 메시지로 400 거부.

`backend/src/mocks/file-validation.util.ts`의 `validateUploadedFile()`이
이 로직을 갖고 있고, `MockStorageService`/`S3StorageService` 양쪽이
그대로 공유한다 (Mock-first 설계 원칙 그대로 유지).

### 2) 용도별 용량 상한 분리 + multer 스트림 단계 차단
기존 `MAX_UPLOAD_SIZE_BYTES = 50MB` 단일 상수를 없애고:
- `MAX_CERTIFICATION_EVIDENCE_SIZE_BYTES = 15MB`
- `MAX_RESULT_FILE_SIZE_BYTES = 20MB`

로 분리했다. `StorageService.saveFile()` 인터페이스가 이제 세 번째
인자로 `maxSizeBytes`를 요구하도록 바뀌었고, 호출하는 쪽(컨트롤러)이
용도에 맞는 상수를 넘긴다.

더 중요한 부분은 **검사 시점**이다. 기존에는 `validateUploadedFile()`이
"파일을 전부 메모리에 다 받은 뒤"에야 용량을 검사했기 때문에, 공격자가
고의로 아주 큰 파일을 보내면 거부되기 전까지 이미 서버 메모리에 전부
올라가는 부담이 있었다. 이제 각 `FileInterceptor`에
`limits: { fileSize: ... }`를 추가해서, **multer 자체가 스트림을 읽는
도중** 그 크기를 넘는 순간 바로 요청을 끊어버리게 했다 - 애플리케이션
레벨 검증은 "이미 통과한 파일이 확장자/매직바이트에 맞는지"만 확인하면
되고, "얼마나 큰 파일을 계속 받아줄지"는 그 앞단(multer)에서 막는
이중 방어 구조.

적용된 업로드 엔드포인트 3곳:
- `POST /certifications` (evidenceFile) → 15MB 한도
- `POST /bounties/:id/submit` (resultFile) → 20MB 한도
- `POST /bounties/:id/milestones/:milestoneId/submit` (resultFile) → 20MB 한도

### 테스트 (실제 API 호출로 검증)
회원가입 → 로그인 → (자격 인증 / 바운티 생성·지원·선정·결제확인) 실제
흐름을 통해 5가지 시나리오를 전부 검증했다:

1. **정상 PDF 업로드 (자격 인증, 15MB 이하)** → 정상 접수, AI OCR Mock까지
   정상 동작 확인.
2. **확장자만 `.pdf`인 가짜 파일 (실제 내용은 평범한 텍스트)** → 매직바이트
   불일치로 400 거부, 새 에러 메시지 확인.
3. **20MB 파일을 자격 인증(15MB 한도)에 업로드** → HTTP 413 `Payload Too
   Large`로 거부, 응답이 0.1초 이내로 매우 빨리 돌아옴 (파일을 다 받고 나서
   거부한 게 아니라 multer가 스트림 단계에서 바로 끊었다는 증거).
4. **정상 PDF 업로드 (바운티 결과물, 20MB 이하)** → 정상 제출, 바운티 상태
   `LOCKED` → `SUBMITTED` 전환 확인.
5. **25MB 파일을 바운티 결과물(20MB 한도)에 업로드** → 마찬가지로 HTTP 413
   으로 즉시 거부 확인.

### 참고: 이번 작업의 사용자 화면 영향
이번 Task #39는 순수 백엔드 검증 로직 강화라 **새로 추가되거나 바뀐
화면 문구/버튼/라벨은 없다** (에러 메시지 문구가 한 가지 더 늘었을 뿐,
기존에도 있던 "허용되지 않는 파일 형식입니다" 같은 에러 메시지와 같은
성격 - 사용자가 정상적으로 올바른 형식/용량의 파일을 올리는 한 절대
마주치지 않는 메시지).

## 22. 보안 강화 3탄 - 로그아웃/비밀번호 변경 시 JWT 서버측 무효화 [완료]

### 배경
기존 JWT는 완전한 "무상태(stateless)" 방식이었다 - 한 번 발급되면 서버가
그 토큰을 강제로 무효화할 방법이 없어서, 로그아웃 버튼을 눌러도 비밀번호를
바꿔도 이미 발급된 토큰은 만료 시간(24h)까지 계속 유효한 출입증으로 쓸 수
있었다. 사용자가 이 부분을 "최소한이 아니라 좀더 복잡하게" 구현해달라고
명시적으로 요청해서, 단순히 프론트에서 로컬 토큰만 지우는 수준이 아니라
서버가 실제로 세션을 추적하고 무효화하는 구조로 만들었다.

### 설계: 완전 무상태 JWT → "서버가 세션을 추적하는" 하이브리드 모델
`AuthSession` 테이블(`backend/.../auth/entities/auth-session.entity.ts`)을
새로 추가했다. 로그인/회원가입으로 토큰을 발급할 때마다:
1. JWT의 `jti`(JWT ID) 클레임에 쓸 UUID를 하나 만든다
2. 그 값을 PK로 하는 `AuthSession` 행을 DB에 저장한다 (`userId`,
   `expiresAt`, `revokedAt: null`)
3. 그 jti를 포함해 JWT를 서명한다

이후 **모든 인증된 요청**에서 `JwtStrategy.validate()`가 서명/만료 검증에
더해 "이 jti에 해당하는 세션이 DB에 아직 살아있는가(`revokedAt`이
비어있는가)"까지 확인한다. 세션이 없거나 revoke된 상태면 서명 자체는
멀쩡해도 401로 거부한다.

### 새로 생긴 기능
- **`POST /auth/logout`**: 지금 요청에 실려온 토큰의 세션 하나만 revoke
  (이 기기에서만 로그아웃). 프론트엔드 `auth-context.tsx`의 `logout()`도
  같이 수정해서, 예전처럼 로컬 토큰만 지우는 게 아니라 이 API를 먼저
  호출해 서버 세션도 실제로 끊도록 연결했다 (안 그러면 백엔드에 만들어둔
  로그아웃 API가 실제 화면에서는 전혀 안 쓰이는 상태로 남았을 것).
- **`POST /auth/logout-all`**: 그 사용자 명의로 발급된 살아있는 모든
  세션을 한꺼번에 revoke ("다른 기기에서 로그인된 걸 원격으로 끊고
  싶을 때"용).
- **`resetPassword()` 강화**: 비밀번호 재설정에 성공하면 자동으로
  `logoutAll()`을 호출해 그 계정의 모든 세션을 강제로 끊는다. "계정이
  탈취됐을 수도 있다"는 신호이므로, 진짜 주인이 비밀번호를 바꾸는 순간
  공격자가 들고 있을지 모르는 기존 토큰도 함께 무효화되어야 하기 때문.

### 참고: 이번 작업의 사용자 화면 영향
새로 추가된 화면 문구는 없다. 기존에 있던 "로그아웃" 버튼(Navbar)은
문구도 위치도 그대로고, 버튼을 눌렀을 때 내부적으로 서버 API를 한 번
더 호출하도록 바뀐 것뿐이라 사용자 눈에 보이는 변화는 없다. 다만
새로 추가된 API 응답 메시지 두 가지는 기록해둔다 (지금은 백엔드
엔드포인트만 있고 "전체 로그아웃" 버튼은 화면에 아직 없음 - 필요하면
추후 마이페이지 등에 노출 가능):
- `POST /auth/logout` 성공 시: `"로그아웃되었습니다."`
- `POST /auth/logout-all` 성공 시: `"모든 기기에서 로그아웃되었습니다."`
- 세션이 무효화된 토큰으로 요청 시 401 메시지: `"세션이 만료되었거나
  로그아웃되었습니다. 다시 로그인해주세요."`

### 테스트 (실제 API 호출로 검증, 총 6단계)
1. 회원가입 → 발급된 토큰으로 `/auth/me` 정상 동작 확인 (JWT payload에
   `jti` 클레임이 실제로 들어가는 것도 디코딩해서 확인)
2. `POST /auth/logout` 호출 → **같은 토큰으로** `/auth/me` 재호출 시 401
   (서명은 멀쩡한데도 세션이 없어서 거부되는 것 확인 - 순수 JWT 만료가
   아니라 우리가 만든 세션 체크가 실제로 동작한다는 증거)
3. 같은 계정으로 두 번 로그인해서(기기 A/B 흉내) 서로 다른 `jti`를 가진
   토큰 두 개 발급 확인, 둘 다 정상 동작 확인
4. 토큰 A로 `POST /auth/logout-all` 호출 → 토큰 A **뿐 아니라** 전혀
   다른 요청에 쓰인 적 없는 토큰 B도 함께 401로 거부되는 것 확인
   (한 사용자의 모든 세션이 실제로 같이 끊기는 것 검증)
5. 새로 로그인해서 유효한 토큰 확보 → `POST /auth/forgot-password` →
   백엔드 로그에서 Mock 이메일로 발송된 재설정 링크의 토큰 추출 →
   `POST /auth/reset-password`로 비밀번호 변경 → **재설정 전에 발급됐던
   토큰**으로 `/auth/me` 재호출 시 401 (재설정이 자동으로 기존 세션을
   끊는 것 확인)
6. 새 비밀번호로 정상 로그인되는 것 / 예전 비밀번호로는 거부되는 것
   확인 (비밀번호 자체도 실제로 바뀌었는지 sanity check)

프론트엔드는 `npx tsc --noEmit`과 `next build` 둘 다 통과 확인, 백엔드는
`nest build` 통과 + 위 6단계 전부 실제 서버 기동 상태에서 curl로 검증.

## 23. 보안 강화 4탄 - 이메일 인증 전 로그인 차단 + 재발송 [완료]

### 배경
`User.emailVerifiedAt` 컬럼과 인증 메일 발송/검증 흐름 자체는 Task #11에서
이미 만들어져 있었지만, 인증을 안 해도 로그인/서비스 이용에 아무 제약이
없었다 (컬럼 주석에도 "Phase 2 시점에는 막지 않는다"고 명시했었다). 사용자가
이 부분을 "통장/계좌를 1인 1계좌로 관리하듯 이메일도 동일하게" 강하게
막아달라고 요청해서, 이번에 실제로 게이트를 걸었다.

### 무엇이 바뀌었나

1. **`login()` - 이메일 미인증 계정은 비밀번호가 맞아도 403 차단**
   `user.emailVerifiedAt`이 비어있으면 `ForbiddenException`을 던진다. 이때
   응답의 `error` 필드를 표준 문구("Forbidden") 대신 `EMAIL_NOT_VERIFIED`라는
   식별 가능한 값으로 채워서, 프론트가 "비밀번호 자체가 틀렸다"(401 +
   일반 메시지)와 "인증만 안 했다"(403 + `EMAIL_NOT_VERIFIED`)를 구분할 수
   있게 했다 (`AllExceptionsFilter`가 이 필드를 그대로 응답에 실어준다).
   **틀린 비밀번호는 여전히 인증 여부와 무관하게 항상 같은 401 메시지를
   반환** - "이 이메일이 인증됐는지 여부"가 비밀번호 대입 공격의 곁가지
   정보로 새어나가지 않게 하기 위함.

2. **`register()` - 가입 직후 자동 로그인 제거**
   예전에는 가입 성공 시 바로 `accessToken`을 함께 내려줘서 사실상
   "가입 = 즉시 로그인"이었다. 이러면 위의 로그인 차단 정책이 가입
   시점에는 그냥 우회된다 (애초에 로그인 절차를 거치지 않고 들어와
   있으니까). 그래서 이제 회원가입은 계정 생성 + 인증 메일 발송까지만
   하고, 토큰 없이 `{ email, message }`만 응답한다. 실제로 서비스를
   쓰려면 이메일 인증 → `/auth/login`을 그대로 거쳐야 한다.

3. **`POST /auth/resend-verification` 신설**
   인증 메일을 못 받았거나 링크가 만료됐을 때 다시 받을 수 있는 엔드포인트.
   `forgot-password`와 완전히 같은 이메일 enumeration 방지 원칙을 그대로
   재사용했다 - 이메일이 가입되어 있는지, 이미 인증됐는지와 무관하게 항상
   똑같은 성공 메시지를 반환하고, 실제로 메일을 (재)발송할지만 서버 내부에서
   조용히 갈린다.

### 화면(프론트엔드) 변경 - 신규/변경된 사용자 노출 문구 정리
사용자님 말씀하신 대로, 이번 작업으로 화면에 새로 노출되는 문구가 꽤
생겨서 전부 정리해서 알려드립니다:

- **회원가입 화면**: 가입 성공 시 더 이상 바로 다른 화면으로 안 넘어가고,
  `forgot-password` 화면과 같은 스타일의 안내 박스가 뜬다.
  - 내용: 백엔드가 돌려준 메시지를 그대로 표시 → `"회원가입이 완료되었습니다.
    {이메일}로 발송된 인증 메일의 링크를 클릭한 후 로그인해주세요."`
  - 그 아래 링크: **"로그인하러 가기"**
- **로그인 화면**: 이메일 미인증으로 막히면(403 `EMAIL_NOT_VERIFIED`), 기존
  에러 문구 아래에 노란색 안내 박스가 추가로 뜬다.
  - 에러 문구(기존 빨간 글씨 자리): `"이메일 인증 후 로그인할 수 있습니다.
    인증 메일을 다시 받으시겠어요?"`
  - 안내 박스 안내문: **"아직 이메일 인증을 안 하셨다면 링크를 다시
    보내드릴 수 있어요."**
  - 버튼: **"인증 메일 재발송"** (누르는 동안 **"발송 중..."**)
  - 재발송 성공 시 그 자리에 표시되는 메시지: `"가입된 이메일이고 아직
    인증 전이라면 인증 메일이 발송되었습니다."`
- **이메일 인증 완료 화면**: 인증 성공/실패와 무관하게 하단 링크가
  "바운티 목록으로"(로그인이 필요한 화면이라 이제 안 맞음) →
  **"로그인하러 가기"**로 바뀜.

### 참고: 프론트엔드 구조 변경
- `lib/api.ts`에 `hasErrorCode(error, code)` 헬퍼를 추가했다. 에러 메시지
  문자열을 파싱하는 대신, 백엔드가 실어 보내는 `error` 필드(예:
  `EMAIL_NOT_VERIFIED`)로 판별해서, 나중에 문구가 바뀌어도 화면 분기
  로직이 안 깨지게 했다.
- `auth-context.tsx`의 `register()` 반환 타입이 `{accessToken, user}`에서
  `{email, message}`로 바뀌었다 (더 이상 로그인 상태를 만들지 않음).

### 테스트 (실제 API 호출로 검증, 총 8단계)
1. 새 계정 회원가입 → 응답에 `accessToken`이 없고 `{email, message}`만
   오는 것 확인
2. 인증 전 상태에서 **정확한** 비밀번호로 로그인 시도 → 403 +
   `error: "EMAIL_NOT_VERIFIED"` 확인
3. **틀린** 비밀번호로 시도 → 인증 여부와 무관하게 기존과 동일한 일반
   401 메시지(정보 누출 없음) 확인
4. 가입된 적 없는 이메일로 `/auth/resend-verification` 호출 → 성공
   메시지는 뜨지만 실제로 메일은 발송되지 않는 것을 로그로 확인
5. 실제 가입된(미인증) 이메일로 재발송 요청 → 4번과 **완전히 동일한
   응답 메시지**(enumeration 방지) + 이번엔 실제로 새 인증 링크가 로그에
   찍히는 것 확인
6. 재발송된 링크의 토큰으로 `/auth/verify-email` 호출 → 인증 완료
7. 인증 완료 후 같은 계정/비밀번호로 로그인 → 이번엔 정상적으로
   `accessToken`(+ Task #40의 `jti` 세션 클레임까지) 발급되는 것 확인
8. 이미 인증된 계정에 다시 재발송 요청 → 같은 성공 메시지가 뜨지만
   실제로는 새 메일이 발송되지 않는 것(로그에 새 항목 없음) 확인

백엔드는 `nest build`, 프론트엔드는 `npx tsc --noEmit` + `next build`
둘 다 통과 확인, 위 8단계는 전부 실제 서버 기동 상태에서 curl로 검증했다.

## 24. 보안 강화 5탄 - 비밀번호 정책 강화 (16자 이상 + 대/소문자 + 특수문자) [완료]

### 배경
기존 비밀번호 규칙은 "8자 이상"이 전부였다. 사용자가 더 강한 정책을
요청해서, 회원가입/비밀번호 재설정 양쪽에 동일하게 적용했다.

### 무엇이 바뀌었나
`backend/src/common/validators/password-policy.ts`를 새로 만들어 규칙을
한 곳에 모았다 (file-validation.util.ts를 여러 곳이 공유하는 것과 같은
이유 - 규칙이 여러 파일에 복사되면 나중에 한쪽만 고치고 잊어버리는 사고가
나기 쉽다):

- **16자 이상**
- **대문자 최소 1개**
- **소문자 최소 1개**
- **특수문자 최소 1개** (영문자도 숫자도 아닌 문자로 정의 - 어떤 특수기호든 인정)
- 숫자 포함은 별도로 강제하지 않음 (요청받은 조건 그대로)

이 규칙(정규식 + 에러 메시지)을 아래 세 곳이 전부 공유한다:
- `RegisterDto.password` (회원가입)
- `ResetPasswordDto.newPassword` (비밀번호 재설정)
- `seed-admin.ts` (관리자 계정 생성 스크립트 - 웹 API를 거치지 않는 별도
  경로라 DTO의 `class-validator` 데코레이터가 적용되지 않아서, 직접
  같은 정규식으로 한 번 더 검사하도록 추가했다. 가장 강한 권한을 가진
  계정이 일반 계정보다 약한 비밀번호를 쓸 이유가 없다는 판단)

### 화면(프론트엔드) 변경 - 신규/변경된 사용자 노출 문구
- **회원가입 화면**: 비밀번호 라벨이 `"비밀번호 (8자 이상)"` →
  `"비밀번호 (16자 이상 · 대문자·소문자·특수문자 포함)"`로 변경. 입력창에
  `pattern` 속성을 걸어서, 조건을 안 만족하면 서버까지 안 가고 브라우저가
  먼저 풍선 메시지로 알려준다: `"16자 이상이며 대문자, 소문자, 특수문자를
  각각 최소 1개 이상 포함해야 합니다"`
- **비밀번호 재설정 화면**: 라벨이 `"새 비밀번호 (8자 이상)"` →
  `"새 비밀번호 (16자 이상 · 대문자·소문자·특수문자 포함)"`로 동일하게 변경,
  같은 방식의 브라우저 유효성 검사 적용
- 브라우저 검사를 우회해서 요청을 직접 보내는 경우(예: API를 직접 호출)를
  대비해, 서버(DTO validation)도 동일한 규칙으로 최종 검사하고 같은
  한국어 에러 메시지를 반환한다: `"비밀번호는 16자 이상이며 대문자, 소문자,
  특수문자를 각각 최소 1개 이상 포함해야 합니다"`

### 테스트 (실제 API 호출로 검증)
1. 기존 8자짜리 비밀번호(대/소문자/특수문자 없음)로 가입 시도 → 400 거부
2. 16자 이상이지만 특수문자 없음 → 400 거부
3. 16자 이상 + 특수문자 있지만 대문자 없음 → 400 거부
4. 16자 + 대문자 + 소문자 + 특수문자를 전부 만족하는 비밀번호로 가입 →
   정상 가입 성공
5. 비밀번호 재설정 요청 → 백엔드 로그에서 재설정 토큰 추출
6. 그 토큰으로 약한 새 비밀번호를 넣어 재설정 시도 → 400 거부 (이 실패로
   토큰이 소모되지 않는 것까지 확인)
7. 같은 토큰으로 조건을 만족하는 새 비밀번호로 재설정 → 성공
8. 프론트엔드도 실제 브라우저(Playwright)로 회원가입 화면에서 약한
   비밀번호를 입력하고 제출을 눌러봐서, 서버로 요청이 가기도 전에
   브라우저가 먼저 막고 정확한 한국어 안내 풍선을 보여주는 것을
   스크린샷으로 확인

백엔드는 `nest build`, 프론트엔드는 `npx tsc --noEmit` + `next build`
둘 다 통과 확인.

## 25. 마이페이지 통합 대시보드 (Task #31) [완료]

### 배경
예전에는 "내가 등록한 바운티"를 보려면 전체 바운티 목록에서 직접 찾아야
했고, "내가 지원한 바운티"는 볼 방법 자체가 없었다(지원 당한 쪽인 의뢰인만
지원자 목록을 볼 수 있었음). 자격 인증 현황도 `/certifications` 화면에,
안 읽은 알림 수도 네비게이션 바 종에 따로 흩어져 있었다. 이번 작업으로
그 전부를 한 화면(`/mypage`)에 모았다.

### 설계 - 왜 새 모듈(DashboardModule)로 분리했나
집계 로직을 어디에 둘지가 이번 작업의 핵심 설계 결정이었다. `users.controller.ts`
상단에는 원래 "마이페이지를 만들 때 여기 엔드포인트가 붙을 자리"라는
주석이 있었지만, 실제로 만들어보니 그럴 수 없었다: `BountiesModule`이
이미 `UsersModule`을 가져다 쓰고 있어서, 만약 집계 로직을 `UsersController`에
두고 `UsersModule`이 거꾸로 `BountiesModule`을 가져오면
`BountiesModule → UsersModule → BountiesModule` 순환 참조가 생긴다.

그래서 아무도 되돌아 참조할 필요가 없는 새 최상위 모듈
`DashboardModule`(`backend/src/modules/dashboard/`)을 만들어
`UsersModule`/`BountiesModule`/`CertificationsModule`/`NotificationsModule`
네 개를 전부 주입받는 방식으로 순환을 피했다. 각 도메인의 실제 조회
로직(필터링, 소유권 확인 등)은 여전히 해당 도메인 서비스에 그대로 두고,
`DashboardService`는 그 결과들을 한 응답으로 "조립"만 한다.

### 새로 생긴 백엔드 기능
- `BountiesService.findMyBountiesAsClient(clientId)` - 내가 의뢰인으로
  등록한 바운티 전체 (기존에는 이런 필터가 전혀 없었다 - 목록 API는 전체
  공개 목록만 지원)
- `BountiesService.findMyApplicationsAsExpert(expertId)` - 내가 전문가로
  지원한 모든 건(선정 전/선정됨/반려됨 전부), `relations: ['bounty']`로
  대상 바운티 정보까지 한 번의 쿼리로 JOIN해서 가져온다
- `GET /api/dashboard/me` (JWT 인증 필요) - 위 두 메서드 + 기존
  `CertificationsService.findMine` + `NotificationsService.countUnread`를
  `Promise.all`로 병렬 조회해서 한 번에 응답. 화면 상단 요약 카드용 집계
  (등록한 바운티 수, 진행중 건수, 지원 건수, 선정 건수, 승인된 인증 수,
  안 읽은 알림 수)도 서버에서 미리 계산해서 내려준다 - 프론트가 배열을
  순회해 계산하지 않아도 되게.

### 실제 테스트 중 발견한 회귀 버그 (중요)
대시보드를 curl로 테스트하려고 관리자 계정을 새로 만들었는데(`seed-admin.ts`),
**로그인이 403으로 막히는 버그를 실제 테스트 중에 발견했다.** 원인:
Task #41(이메일 인증 전 로그인 차단)을 추가하면서, 회원가입(`register()`)
경로는 인증 메일 발송 로직이 있어 문제없지만, `seed-admin.ts`는 그 경로를
거치지 않고 DB에 직접 사용자를 꽂아 넣는 별도 스크립트라 `emailVerifiedAt`이
항상 비어있었다 - 그래서 관리자로 승격해도 로그인 자체가 막혀버렸다.

수정: 관리자 계정은 "서버 콘솔에 직접 접근 가능한 사람만 만들 수 있는" 이미
더 강한 신뢰 경로로 생성되는 것으로 보고, `seed-admin.ts`가 계정을
만들거나 승격할 때 `emailVerifiedAt`을 자동으로 채우도록 했다. (다른
사용자 생성 경로가 더 있는지 전체 코드베이스에서 `usersService.create`/
`userRepo.create`를 검색해 확인 - `register()`와 `seed-admin.ts` 두 곳뿐인
것 확인.) 이런 걸 코드 리뷰만으로는 못 잡고, 실제로 스크립트를 실행하고
로그인까지 시도해봐서 잡은 버그다.

### 프론트엔드 - 신규 화면 `/mypage`
- 상단에 프로필(이름/이메일) + 요약 통계 6칸(등록한 바운티/진행중/지원한
  바운티/선정됨/승인된 인증/안 읽은 알림)
- 내가 등록한 바운티 목록 (기존 `BountyStatusBadge` 재사용)
- 내가 지원한 바운티 목록 (새 `ApplicationStatusBadge` 컴포넌트 추가 -
  지원함/선정됨/선정되지 않음 3가지 상태를 배지로 표시)
- 내 자격 인증 현황 (기존 `/certifications` 페이지와 같은 상태 배지 스타일)
- 각 목록이 비어있으면 "아직 없어요" + 해당 화면으로 가는 링크 안내
- `Navbar`에 "마이페이지" 링크 추가

사용자님이 "UI는 나중에 따로 시간 내서" 다듬기로 하셔서, 지금은 기존
페이지들(바운티 목록, 인증 신청)의 스타일 클래스를 그대로 재사용하는
수준으로만 만들고 디자인 다듬기는 하지 않았다.

### 테스트 (실제 API + 실제 브라우저로 검증)
1. 활동이 전혀 없는 새 계정으로 `GET /dashboard/me` 호출 → 모든 목록이
   빈 배열, 모든 카운트가 0인 것 확인
2. 그 계정으로 바운티 하나를 의뢰인으로 등록 → 재조회 시
   `clientBounties`에 1건, `summary.clientBountyCount`도 1로 반영되는 것 확인
3. 다른 계정(별도 의뢰인)이 만든 바운티에 전문가로 지원 → 재조회 시
   `expertApplications`에 1건, 그 안의 `bounty` 필드에 대상 바운티 정보가
   정확히 JOIN되어 들어있는 것 확인
4. 자격 인증 1건 제출(자동 승인) → `certifications`와
   `summary.certificationApprovedCount`에 반영 확인
5. 인증 없이 `/dashboard/me` 호출 → 401 확인 (가드 정상 동작)
6. 실제 헤드리스 브라우저(Playwright)로 로그인 → 마이페이지 이동 →
   콘솔 에러 없이 위에서 만든 바운티 제목/지원 내역/인증 번호가 화면
   텍스트에 실제로 렌더링되는 것까지 확인 (스크린샷은 남기지 않음 -
   UI는 나중에 다듬기로 한 방침에 따름)

백엔드는 `nest build`, 프론트엔드는 `npx tsc --noEmit` + `next build`
둘 다 통과 확인.

## 26. Jest 유닛/e2e 테스트 코드 (Task #32)

### 왜 "유닛 테스트만"으로는 부족한가 - 테스트 전략을 둘로 나눈 이유

`BountiesService`(751줄)는 지원자 선택/결제확인/정산/마일스톤 로직 전부를
`this.dataSource.transaction(async (manager) => {...})`로 감싸서 여러
테이블(Bounty/BountyApplication/Transaction)에 걸친 쓰기를 원자적으로
묶는다(Task #26). 이 트랜잭션 경계를 리포지토리를 전부 mock으로 흉내내서
유닛테스트로 검증하려면, TypeORM의 QueryRunner/EntityManager 내부 동작까지
가짜로 만들어야 해서 "진짜로 그 로직이 맞는지"를 확인하는 효과가 오히려
떨어진다(모킹 자체가 틀렸는데 테스트만 통과하는 상황을 만들기 쉬움).

그래서 테스트를 성격에 따라 두 계층으로 나눴다:
- **순수 함수 / 트랜잭션 없는 서비스 → 유닛 테스트** (리포지토리 mock,
  DB 연결 없이 수백 ms 안에 끝남)
- **여러 테이블에 걸친 트랜잭션 상태머신 → e2e 테스트** (실제 테스트
  DB에 진짜 HTTP 요청을 끝까지 흘려보내서 검증)

이 프로젝트가 지금까지 한 세션 내내 실제로 curl/Playwright로 직접
테스트해온 방식과도 방향이 같다 - "진짜로 동작하는가"를 최종적으로
확인하는 게 이 프로젝트 전체의 검증 원칙이었다.

### 테스트 DB 분리 (`credobounty_test`)

개발 중 수동으로 만들어둔 계정/바운티 데이터가 테스트 실행마다 섞이거나
지워지는 사고를 막기 위해, 개발용 `credobounty` DB와 완전히 분리된
`credobounty_test` DB를 별도로 만들었다.

- `backend/.env.test` (신규, git에 커밋됨) - `DB_NAME=credobounty_test`,
  전용 `JWT_SECRET`(운영값과 무관, 유출돼도 무방), `PAYMENT_GATEWAY_DRIVER=mock`
  등 테스트 전용 값만 담음. `.env`(운영/개발용)는 `.gitignore`에 있지만
  `.env.test`는 값 전부가 테스트 전용이라 의도적으로 커밋해서, 이 저장소를
  누가 새로 clone해도 별도 설정 없이 바로 `npm run test:e2e`가 돌아가게 했다.
- `backend/test/setup-env.ts` (신규) - Jest의 `setupFiles`로 지정해서,
  `AppModule`이 import되기(=`TypeOrmModule.forRootAsync`가 DB 접속 설정을
  읽기) 전에 가장 먼저 실행되게 했다. `dotenv.config()`는 이미 설정된
  `process.env` 값을 덮어쓰지 않는 기본 동작이라, 여기서 먼저
  `.env.test` 값을 채워두면 이후 `ConfigModule.forRoot()`의 `.env` 로드가
  이 값들을 건드리지 못한다.
- `backend/test/jest-e2e.json` (신규) - `testRegex: ".e2e-spec.ts$"`,
  `setupFiles: ["<rootDir>/setup-env.ts"]`, `testTimeout: 30000`(트랜잭션
  여러 개 + HTTP 요청 여러 번 이어지는 테스트라 기본 5초로는 부족할 수 있어 여유를 둠).
- 테스트 DB 자체(`credobounty_test`)는 `createdb`로 최초 1회만 만들면 되고,
  스키마는 `synchronize: true`(개발 단계 전용 설정)라 앱이 뜨면서 자동 생성된다.
- 실제로 e2e 테스트를 돌린 뒤 `psql`로 두 DB를 직접 조회해서, 테스트 데이터가
  `credobounty_test`에만 들어가고 `credobounty`(개발 DB)에는 전혀 영향이
  없는 것까지 확인했다.

### 작성한 테스트 목록

**순수 유닛 테스트 (mock, DB 없음) - 42개, 전부 수 초 안에 통과**
- `src/mocks/settlement-fee.util.spec.ts` - 플랫폼 수수료(10%)/의뢰인 단순변심
  취소 수수료(3%) 계산, 특히 `Math.floor` 내림 처리가 소수점 발생 케이스에서
  정확히 되는지
- `src/common/validators/password-policy.spec.ts` - 16자 이상 + 대/소문자 +
  특수문자 정규식의 전방탐색 4개를 각각 하나씩만 어긴 케이스로 검증(숫자는
  요구사항에 없으므로 숫자 없이도 통과해야 함을 포함)
- `src/common/config/jwt-secret.util.spec.ts` - `getRequiredJwtSecret()`이
  "약한 시크릿으로 조용히 뜨는 것"을 막는 함수라, 언제 예외를 던지는지(없음/
  공백/플레이스홀더 값/32자 미만)가 테스트의 핵심. 32자 경계값(31자 vs 32자)도 확인
- `src/mocks/file-validation.util.spec.ts` - 실제 매직바이트를 가진 최소
  버퍼로 PDF/PNG/ZIP 통과, 허용 안 된 확장자 거부, 그리고 이 유틸의 존재
  이유인 "확장자만 위장한 파일"(exe를 .png로 바꿔치기) 거부까지 확인.
  용량 제한 경계값(정확히 제한값 vs 1바이트 초과)도 확인
- `src/modules/auth/auth.service.spec.ts` - 리포지토리/JwtService/Mock
  서비스들을 전부 mock으로 대체한 `@nestjs/testing` 유닛테스트. 이번
  세그먼트에서 추가한 보안 로직 3가지가 실제 코드 경로대로 동작하는지에
  집중: (1) 가입 시 토큰 미발급 (2) 이메일 미인증 계정은 비번이 맞아도
  `ForbiddenException({error: 'EMAIL_NOT_VERIFIED'})` (3) 비밀번호 재설정 시
  `logoutAll` 호출로 기존 세션 전부 revoke

**e2e 테스트 (실제 테스트 DB + 실제 HTTP 요청) - 24개**
- `test/auth.e2e-spec.ts` (11개) - 회원가입(토큰 없음 확인) → 이메일
  미인증 상태 로그인 차단(403 + `EMAIL_NOT_VERIFIED`) → 인증 → 로그인
  성공 → `/auth/me` → 로그아웃 → **로그아웃한 토큰으로 재요청 시 401**
  (서버측 세션 무효화가 실제로 동작하는지) → 비밀번호 재설정 →
  **재설정 전에 발급되어 있던 다른 세션도 같이 revoke되는지** → 새
  비밀번호로만 로그인 성공, 옛 비밀번호는 실패 → 약한 비밀번호 가입 400
  거부. `MockEmailService`를 `overrideProvider`로 "보낸 링크를 배열에
  저장하는" 가짜로 바꿔치기해서, 실제 메일 서버 없이도 실제로 발급된
  인증/재설정 토큰 값을 꺼내 흐름을 이어갈 수 있게 했다.
- `test/bounty-flow.e2e-spec.ts` (13개) - 의뢰인/전문가/제3자 3개 계정
  가입+인증 → 전문가 자격 인증 신청(증빙서류 없이 형식 검증만으로 자동
  승인) → 자격 없는 사용자의 지원 차단(403) → 바운티 등록 → 지원 →
  **바운티 주인이 아닌 사람의 선택 시도 차단(403)** → 지원자 선택(결제
  대기 전환 + paymentId 발급) → 결제 확인(LOCKED 전환, Mock PG는 항상
  성공) → **무관한 사용자의 결과물 제출 차단(403)** → 선택된 전문가의
  결과물(zip) 제출(SUBMITTED 전환, 매직바이트 검증까지 실제 통과) →
  **바운티 주인이 아닌 사람의 승인 시도 차단(403)** → 의뢰인 승인
  (SETTLED 전환) → `/transactions/by-bounty/:id`로 정산 내역 조회해서
  플랫폼 수수료(10%)가 정확히 계산됐는지까지 확인 → 이미 정산된 바운티
  재승인 시도 400 거부.

### 테스트하면서 실제로 발견한 것

1. **테스트 버그 (앱 코드는 정상)**: `amount`/`platformFeeAmount`/
   `settledAmount` 컬럼은 DB에서 bigint라 TypeORM/pg가 JS number 정밀도
   손실을 피하려고 문자열로 내려준다(`transactions.service.ts`의
   `settleMilestone` 주석에 이미 남겨뒀던 것과 같은 이유). 처음 테스트를
   `toBe(200000)`로 짰다가 실제로는 `"200000"`(문자열)이라 실패했고,
   `Number()`로 변환하도록 고쳐서 해결 - 실제 앱 동작에는 문제가 없었다.
2. **아이디어로 남겨두는 사소한 순서 이슈** (보안 취약점은 아님): `POST
   /bounties/:id/submit`과 `/milestones/:milestoneId/submit`의 컨트롤러가
   "파일을 첨부했는가"(400) 검사를 서비스 레이어의 "이 바운티에 선택된
   전문가가 맞는가"(403) 검사보다 먼저 한다. 그래서 권한이 없는 사용자가
   파일 없이 제출을 시도하면 403이 아니라 400이 먼저 뜬다. 정보 노출이나
   보안 구멍은 아니지만(어차피 둘 다 거부됨), 프론트 입장에서는 "권한이
   없어서 막힌 건지 파일을 안 보내서 막힌 건지"가 에러 코드만으로는
   헷갈릴 수 있다 - 컨트롤러의 파일 존재 검사를 서비스 레이어 안으로
   옮기거나, 파일 유무와 무관하게 권한 검사를 먼저 하도록 순서를 바꾸면
   더 명확해질 것 같다. 지금 당장 고칠 필요는 없어 보여 백로그로만
   남겨둔다.

### 실행 방법
```
npm test        # 유닛 테스트 (DB 불필요, src/ 아래 *.spec.ts)
npm run test:e2e   # e2e 테스트 (credobounty_test DB 필요, test/ 아래 *.e2e-spec.ts)
```
둘 다 실제로 실행해서 전부 통과(유닛 42개 + e2e 24개 = 총 66개) 확인했고,
e2e 실행 후 `psql`로 개발 DB(`credobounty`)에는 테스트 데이터가 전혀
섞이지 않은 것까지 직접 확인했다.

## 27. GitHub Actions CI (Task #33)

### 왜 지금(GitHub 저장소 연결 전에) 먼저 만들었나

Task #34(GitHub 저장소 연결)보다 순서가 앞서 보일 수 있지만, CI 워크플로우
파일(`.github/workflows/ci.yml`) 자체는 저장소에 커밋되는 "코드"라서 먼저
만들어 검증해두면, 다음에 실제로 GitHub에 push하는 순간부터 바로 동작한다.
반대로 저장소부터 연결하고 CI가 없으면 그 사이에 push되는 커밋들이 아무
자동 검증 없이 올라간다.

### 구성 (`.github/workflows/ci.yml`)

`push`/`pull_request`(main 브랜치 대상)마다 두 잡을 병렬로 돈다 - 이
저장소가 `backend/`·`frontend/`로 완전히 분리된 모노레포 구조와 자연스럽게
맞아떨어진다.

- **backend 잡**: `postgres:16` 서비스 컨테이너를 띄우고(`POSTGRES_USER=postgres`
  / `POSTGRES_PASSWORD=postgres` / `POSTGRES_DB=credobounty_test` - Task
  #32에서 커밋해둔 `backend/.env.test`의 DB 접속값과 반드시 일치해야 하는
  부분이라 워크플로우 파일에 주석으로 남겨뒀다) `pg_isready`로 준비될 때까지
  기다린 뒤, `npm ci` → `npm run build`(컴파일 자체가 깨지지 않는지) →
  `npm test`(mock 기반 유닛 42개) → `npm run test:e2e`(실제 DB에 대고
  인증+바운티 에스크로 전체 흐름 24개)까지 순서대로 실행한다.
- **frontend 잡**: `npm ci` → `npx tsc --noEmit` → `npm run build`(next
  build) - 지금까지 이 프로젝트를 로컬에서 검증할 때 계속 써온 것과 정확히
  같은 두 명령이다.
- 같은 브랜치(PR)에 커밋을 연달아 푸시하면 `concurrency` 설정으로 먼저
  시작된 낡은 실행을 자동 취소해서 CI 리소스를 아낀다.

### 의도적으로 뺀 것 - `npm run lint`(frontend)

frontend의 `package.json`에는 `"lint": "next lint"` 스크립트가 이미
있었지만, 정작 eslint 자체는 devDependency로 설치되어 있지 않았다(확인해보니
`.eslintrc`/`eslint.config.*` 파일도 없음). 이 상태에서 `next lint`를 그대로
실행하면 Next.js가 "eslint를 처음 쓰는 것 같은데 설정을 만들까요?"라고
묻는 대화형 초기설정 프롬프트를 띄우는데, CI처럼 사람이 답할 수 없는
환경에서는 그냥 멈춰버린다(정확히는 응답 없이 타임아웃/실패). 그래서
지금 CI에는 lint 단계를 넣지 않았고, 워크플로우 파일에 이유와 함께
"eslint 도입은 별도 작업"이라고 주석으로 남겨뒀다 - 나중에 eslint를
정식으로 붙일 때 CI에 한 줄만 추가하면 된다.

### 검증

GitHub Actions 러너 자체에서 직접 돌려볼 수는 없는 환경이라(저장소가
아직 원격에 연결되지 않음 - Task #34), 워크플로우가 실행하는 각 명령을
로컬에서 실제로 그대로 실행해 전부 통과하는 것을 확인했다:
`backend`에서 `npm run build` / `npm test` / `npm run test:e2e` 전부
성공, `frontend`에서 `npx tsc --noEmit` / `npm run build`(정적 페이지
13개 생성까지) 전부 성공. YAML 문법 자체도 `python3 -c "yaml.safe_load(...)"`
로 파싱 에러 없는지 확인했다. 다음 Task #34에서 실제로 GitHub에 push하면
이 워크플로우가 진짜 러너에서 처음 실행되는 순간을 확인할 수 있다.

## 28. /api/bounties 500 에러 원인 규명 + 전체 기능 회귀 QA (2026-09-17)

### 배경
어제(2026-09-16) 결제 확인 게이트 관련 작업(`Transaction.paymentId` 컬럼
추가 등)을 마친 직후부터 `GET /api/bounties`를 포함한 DB를 쓰는 모든 API가
500 에러를 내기 시작했다. 원인을 못 찾은 채로 하루를 마쳤고, 오늘 Claude와
함께 처음부터 다시 원인을 찾았다.

### 진짜 원인: `Transaction.paymentId`의 타입 추론 실패
`transaction.entity.ts`에 새로 추가한 컬럼이 아래처럼 타입을 명시하지 않고
있었다.
```ts
@Column({ nullable: true })
paymentId: string | null;
```
TypeORM은 `@Column()`에 `type`을 안 주면 TypeScript의 `design:type`
리플렉션 메타데이터로 컬럼 타입을 추론하는데, `string | null` 같은 합집합
타입은 컴파일 시 `Object`로 뭉개져서 내려온다. Postgres 드라이버는 `Object`
타입을 컬럼으로 만들 수 없어서 `DataTypeNotSupportedError`를 던지고,
그 결과 앱 시작 시점에 `DataSource.initialize()` 자체가 실패한다. DB 연결이
아예 안 되니 `/api/bounties`뿐 아니라 DB를 만지는 모든 엔드포인트가 500을
낸 것 — 어제 이 컬럼을 추가한 그 순간부터 증상이 시작된 이유가 정확히 설명됨.

**수정**: `@Column({ type: 'varchar', nullable: true })`로 타입을 명시.
클라우드 샌드박스에 실제 코드를 그대로 가져와 재현 → 수정 → 재현 안 됨까지
확인 후 실제 저장소 파일에 반영.

**교훈**: `string | null`, `Date | null`처럼 유니언 타입을 쓰는 TypeORM
컬럼은 항상 `type`을 명시적으로 지정할 것 (이미 `receiverId: string | null`
컬럼은 `type: 'uuid'`를 명시해뒀던 것과 비교하면, 이번 컬럼만 빠뜨린 실수였다).

### 회귀 발견: 이의제기 환불 처리 시 바운티 상태가 REFUNDED로 전환되지 않음
26번 섹션(전문가 평판 점수)에는 이 버그를 "발견하고 고쳤다"고 기록돼 있었지만,
오늘 실제 코드를 curl로 끝까지 재현해보니 `DisputesService.resolve()`의
`refund=true` 분기에 `bountiesService.markRefundedAfterDispute(...)` 호출이
빠져 있어서, 환불 처리된 바운티가 여전히 `DISPUTED`에 영원히 멈춰 있었다
(환불 아닌 `refund=false` 분기는 `markSettledAfterDispute()`를 정확히
호출하고 있어서 비대칭이었다). 프론트(`lib/types.ts`, `StatusBadge.tsx`)는
이미 `REFUNDED` 상태와 배지 스타일을 갖추고 대기 중이었는데, 백엔드가 실제로
그 상태를 만들어준 적이 없었던 것 — 문서(26번 섹션)와 실제 코드가 어긋나 있던
사례. 어느 시점엔가 이 한 줄이 유실된 것으로 보인다(원인 불명).

**수정**: `BountiesService.markRefundedAfterDispute()`를 추가하고
`DisputesService.resolve()`의 refund 분기에서 호출하도록 연결. 새 바운티로
결제→제출→이의제기→환불 전 과정을 curl로 재현해 `REFUNDED`로 정확히
전환되는 것, `GET /api/bounties`가 계속 정상 응답하는 것까지 확인.

### 오늘 추가로 실제 재현 테스트한 것 (전부 정상 동작 확인, 버그 없음)
- 결제 확인 게이트 전체 흐름: 지원→선택(PAYMENT_PENDING)→결제 확인
  (confirm-payment)→LOCKED, 매 단계마다 `GET /api/bounties` 정상 응답
- 결과물 제출(PDF, 매직바이트 검증 통과)→승인→SETTLED→평가(rate)→공개
  거래 사례(`/api/cases`)에 익명화되어 정상 노출
- 마일스톤 분할 정산 3단계: 순서대로 제출/승인, 마지막 단계에서 자동 SETTLED
  전환 + 플랫폼 수수료(10%) 정확히 계산됨
- 이의제기 → 관리자 정상 정산(refund=false, RESOLVED_SETTLE) 경로
- AI 인사이트(`/api/ai-insights/me`, 의뢰인/전문가 둘 다), 마이페이지
  대시보드(`/api/dashboard/me`), 전문가 평판 점수(`/api/users/:id/reputation`),
  관리자 정산 스케줄러 수동 실행(`/api/bounties/settlement/run-now`) 전부 정상
- 프론트엔드 실제 화면(로그인/회원가입/바운티 목록·상세/결제/공개 사례/
  마이페이지/AI 인사이트/관리자 이의제기 화면, `/concept-b`·`/concept-c`
  디자인 시안 포함)을 헤드리스 브라우저로 직접 캡처해서 크래시 없이 렌더링
  되는 것까지 확인

### 정리하다가 발견한 것 - 안 쓰는 코드
`backend/src/payments/`(`PaymentsController`, `PortOneService`,
`PORTONE_SERVICE` 토큰)는 `mocks/payment-gateway.interface.ts` 기반의
`PaymentGatewayService` 패턴이 자리잡기 전에 만들어졌던 초기 시도로 보이며,
지금은 어디서도 호출되지 않는 죽은 코드다 (프론트는 `/bounties/:id/select`
→`/bounties/:id/confirm-payment`만 호출하고, `/api/payments/portone/confirm`은
쓰지 않음). 기능상 문제는 없지만 포트폴리오 코드 리뷰 시 헷갈릴 수 있어
백로그로 남겨둔다 - 다음에 정리할 때 `PaymentsModule` 전체를 삭제하거나,
왜 남겨뒀는지 주석으로 명시할 것.

### README.md 갱신
`README.md`의 "핵심 흐름"이 여전히 결제 확인 게이트 이전(지원자 선택 즉시
LOCKED) 버전으로 남아 있었고, 프론트엔드 빠른 시작 섹션 자체가 없었다.
결제 확인 게이트/마일스톤/이의제기 환불·정산 분기/공개 거래 사례/평판/
AI 인사이트를 반영해 흐름을 갱신하고, 프론트엔드 `npm install && npm run dev`
안내를 추가했다.
